// 第 13 章 · 组合与进阶 —— 题单与测验数据。
// 题单围绕五台"组合机器"选题:LRU/LFU(哈希+链表)、前缀和对照组、
// 线段树/树状数组、跳表,外加一道回头串联哈希章的设计题。
// hint 只给方向不剧透,key 用一段话把最优解讲透。

import type { Problem } from "@/lib/problems";
import type { QuizItem } from "@/lib/quiz";

export const PROBLEMS: Problem[] = [
  {
    lc: 303,
    title: "区域和检索 - 数组不可变",
    d: "easy",
    tags: ["前缀和", "选型对照组"],
    hint: "数据加载后永远不改、只查区间和 —— 需要动用线段树这种重型武器吗?先想想最便宜的方案。",
    key: (
      <>
        前缀和:pre[i] = 前 i 个元素之和,sumRange(l, r) = pre[r+1] − pre[l]。
        预处理 O(n),此后每次查询 O(1),实现三行。把它和 LC 307 放在一起看:
        「改不改」这一个条件的变化,让答案从三行前缀和升级成一棵线段树 ——
        选型题的出题逻辑就藏在这对姊妹题里。
      </>
    ),
  },
  {
    lc: 705,
    title: "设计哈希集合",
    d: "easy",
    tags: ["设计", "数组+链表", "哈希回顾"],
    hint: "回头串联第 6 章:桶数组 + 链地址法,你完全有能力从零手搓一个。",
    key: (
      <>
        取一个质数当桶数(比如 769),每个桶挂一条链表(或动态数组):
        hash(key) = key % 769 定位桶,桶内线性查找处理冲突。add / remove /
        contains 平均 O(1)。用本章的眼光重看:哈希集合本身就是「数组 + 链表」
        拼出来的组合机器 —— 你其实第 6 章就见过组合设计了。
      </>
    ),
  },
  {
    lc: 146,
    title: "LRU 缓存",
    d: "medium",
    tags: ["哈希+双向链表", "O(1) 设计", "本章重头戏"],
    hint: "get/put 都要 O(1):一个结构管「在哪」,一个结构管「多旧」,谁也替代不了谁。",
    key: (
      <>
        哈希表存 key → 链表节点引用(瞬移定位),双向链表按访问序排列
        (头新尾旧,维护顺序)。get 命中就把节点摘下来插回头部;put 超容量就摘掉
        tail.prev 并<b>同步删除</b>它的哈希条目。哑头哑尾省掉全部判空。
        必须双向:删除节点要改前驱的 next,单链表拿不到前驱。§02 有完整推导 +
        逐行实现 + 交互实验室。
      </>
    ),
  },
  {
    lc: 304,
    title: "二维区域和检索 - 矩阵不可变",
    d: "medium",
    tags: ["二维前缀和", "容斥"],
    hint: "把一维前缀和推广到二维:大矩形减掉上边一条、左边一条 —— 但有一块被减了两次。",
    key: (
      <>
        pre[i][j] = 以 (0,0) 到 (i−1,j−1) 为对角的矩形和。任意子矩形 =
        pre[r2+1][c2+1] − pre[r1][c2+1] − pre[r2+1][c1] + pre[r1][c1]:
        减上、减左,左上角被减了两次,容斥补回来。预处理 O(mn),查询 O(1)。
        建表时同样用容斥:pre[i][j] = 元素 + 上 + 左 − 左上。
      </>
    ),
  },
  {
    lc: 307,
    title: "区域和检索 - 数组可修改",
    d: "medium",
    tags: ["线段树", "树状数组", "改+查"],
    hint: "又要改又要查,前缀和一改就要 O(n) 重建 —— 本章两台「区间机器」任选其一。",
    key: (
      <>
        线段树:update 从叶到根、query 三种相交情况,双 O(log n)(§04 逐行实现)。
        树状数组:码量只有四分之一 —— update 先算 delta = val − a[i],再沿 i +=
        lowbit(i) 一路加上去;查询沿 i −= lowbit(i) 拼前缀和,区间和 = 两次前缀相减。
        精讲 B 有两种解法的完整对照。
      </>
    ),
  },
  {
    lc: 460,
    title: "LFU 缓存",
    d: "hard",
    tags: ["频次分桶", "双哈希", "minFreq"],
    hint: "比 LRU 多一个维度:先比频次,频次相同再比「最近」。给每个频次开一个桶试试。",
    key: (
      <>
        三张表:key → (val, freq);freq → 该频次的有序桶(桶内按时间序,天然是小
        LRU);再加一个 minFreq 变量。访问 = 把 key 从 freq 桶搬进 freq+1 桶;
        淘汰 = 掐掉 minFreq 桶里最老的 key。minFreq 不用搜索:旧桶被搬空时 +1,
        插入新 key 时归 1。所有操作 O(1)。§03 有分桶图解和三语言核心实现。
      </>
    ),
  },
  {
    lc: 315,
    title: "计算右侧小于当前元素的个数",
    d: "hard",
    tags: ["树状数组", "离散化", "倒序扫描"],
    hint: "从右往左扫,问题就变成:「已经出现过的数里,比我小的有几个?」—— 一个动态的计数前缀和。",
    key: (
      <>
        先把数值离散化成排名 1..n(去重排序后二分定位)。从右到左遍历:ans[i] =
        query(rank − 1)(已登记的、比它小的个数),然后 add(rank, 1) 把自己登记进去。
        「边插入、边问前缀和」正是树状数组的主场,O(n log n)。归并排序统计逆序的思路
        也能解,但 BIT 版短得多。
      </>
    ),
  },
  {
    lc: 1206,
    title: "设计跳表",
    d: "hard",
    tags: ["跳表", "随机化", "多层索引"],
    hint: "有序链表 + 多层「高速公路」:每层从最高往下,向右走、过头就下楼。插入时抛硬币定层数。",
    key: (
      <>
        节点存一个 next[] 指针数组(长度 = 它的层数)。search 从顶层贪心右移、
        过头下楼;add 先像 search 一样走一遍、记下每层的「拐弯点」update[i],
        再逐层做普通链表插入(层数由抛硬币决定:每次 1/2 概率再长一层);
        erase 同理逐层绕过目标节点。期望 O(log n),§06 有完整三语言实现。
      </>
    ),
  },
];

export const QUIZ: QuizItem[] = [
  {
    type: "choice",
    q: "手写 LRU 时,哈希表已经能 O(1) 定位到链表节点了,为什么链表还必须是双向的?",
    opts: [
      "删除节点要改前驱的 next —— 单链表拿到节点却拿不到前驱,找前驱要从头扫 O(n);双向链表自带 prev,摘除 O(1)",
      "双向链表查找元素比单链表快一倍",
      "双向链表更省内存",
      "只是编码习惯,单链表也能做到全部 O(1)",
    ],
    correct: 0,
    wrong: [
      undefined,
      "查找从来不是链表的活 —— 定位靠哈希表。链表只负责维护新旧顺序和 O(1) 摘除/插入,双向与否不改变查找速度。",
      "恰恰相反:每个节点多存一个 prev 指针,内存是变多的 —— 多花的内存买的是 O(1) 摘除。",
      "卡点在 get:把命中节点搬到头部要先摘除它,摘除要改前驱的 next,而单链表找前驱只能从头扫 O(n)。(「拷贝后继值再删后继」的邪招会让哈希表里存的节点引用集体失效,得不偿失。)",
    ],
    why: "哈希表管「在哪」,链表管「多旧」。摘除一个节点要同时改前驱和后继的指针,只有双向链表能 O(1) 拿到前驱 —— 这正是两个结构拼装的接缝处,也是面试官最爱追问的一步。",
  },
  {
    type: "choice",
    q: (
      <>
        Java 里 <code>new LinkedHashMap&lt;&gt;(cap, 0.75f, true)</code> 加一个{" "}
        <code>removeEldestEntry</code> 覆写,就是一个现成的 LRU。它能做到这一点,
        靠的是内部本来就维护着什么?
      </>
    ),
    opts: [
      "一张哈希表 + 一条串起全部条目的双向链表(accessOrder=true 时按访问序排列)",
      "一棵红黑树,按访问时间排序",
      "一个数组,每次访问后重新排序",
      "一个后台定时器,定期扫描并删除最久未用的条目",
    ],
    correct: 0,
    wrong: [
      undefined,
      "红黑树是 TreeMap 的事(第 8 章)。LinkedHashMap = HashMap 的哈希桶 + 每个 Entry 额外的 before/after 指针连成双向链表,不涉及任何树。",
      "每次访问都重排数组是 O(n log n),违背 LRU 的 O(1) 初衷;它实际只是把该 Entry 在链表里摘下来接到尾端,O(1)。",
      "LRU 的淘汰发生在 put 导致超容量的那一刻,是同步的结构操作;removeEldestEntry 在每次 put 后被调用,与定时器无关。",
    ],
    why: "LinkedHashMap 的每个 Entry 除了挂在哈希桶里,还有 before/after 指针把所有条目串成双向链表 —— 和我们手写的 LRU 是同一台机器,只是 JDK 出厂前帮你焊好了。",
  },
  {
    type: "choice",
    q: "数据一旦加载就永不修改,之后只有大量的区间和查询 —— 应该选哪个结构?",
    opts: [
      "前缀和数组:预处理 O(n),每次查询 O(1),实现只要几行",
      "线段树:功能最强,永远是更好的选择",
      "树状数组:比线段树短,肯定更合适",
      "不预处理,每次现场累加",
    ],
    correct: 0,
    wrong: [
      undefined,
      "线段树查询是 O(log n),还要 4n 空间和几十行代码 —— 数据不改时,它对前缀和没有任何优势。「能力越强越好」不成立,匹配需求才是选型。",
      "树状数组同样是为「改」而生的,查询也是 O(log n)。不改数据时它和线段树一样是杀鸡用牛刀,而且还输给前缀和的 O(1)。",
      "每次查询 O(n),查询一多立刻爆炸 —— 前缀和用一次 O(n) 预处理换终身 O(1) 查询,这笔买卖必须做。",
    ],
    why: "选型看操作组合:只查不改 → 前缀和;又改又查 → 线段树 / 树状数组。LC 303(不可变)与 LC 307(可修改)就是专门考这个对照的姊妹题。",
  },
  {
    type: "fill",
    q: (
      <>
        树状数组的灵魂:用一个位运算表达式取出 x 二进制<b>最低位的 1</b>
        (即 lowbit)。写出这个表达式(用 x 表示变量)。
      </>
    ),
    placeholder: "形如 x?x 的位运算表达式…",
    answers: ["x&(-x)", "x&-x", "(-x)&x", "-x&x", "x&(~x+1)", "(~x+1)&x"],
    hint: "提示:-x 在补码里等于「按位取反再 +1」,这个操作恰好保留最低位的 1、清零其余所有位。拿 6 = 0110 试一试。",
    why: "答案是 x & (-x)。补码下 -x = ~x + 1:取反让最低位的 1 变 0、其后全变 1,再 +1 的进位恰好停在原最低位 1 的位置,于是与运算只剩那一位。例:6 = 0110,-6 = 1010,按位与 = 0010 = 2。",
  },
  {
    type: "choice",
    q: "跳表插入新节点时用「抛硬币」决定它有几层索引,而不是精确维护「每 2 个节点抽 1 个」的完美结构。为什么?",
    opts: [
      "完美结构一经插入/删除就被破坏,精确修复要 O(n);随机化让结构在概率上保持平衡,插入只花 O(log n),零维护成本",
      "抛硬币的计算比按位置计数更快",
      "随机层数能把查询优化到 O(1)",
      "为了节省内存",
    ],
    correct: 0,
    wrong: [
      undefined,
      "省的不是掷硬币那点时间 —— 是省掉了插入之后重排所有后续节点索引层的 O(n) 维护成本。",
      "查询的期望复杂度仍是 O(log n)。随机化换来的是「免维护」,不是更快的查询。",
      "每个节点的期望层数是 1 + 1/2 + 1/4 + … ≈ 2,和严格抽样版差不多 —— 内存不是动机,免维护才是。",
    ],
    why: "严格的「上层 = 下层隔一抽一」一插入就全乱,修复要挪动一整片索引。抛硬币让每个节点独立决定层数(每层晋升概率 1/2),整体期望高度 log₂n,查询/插入期望 O(log n) —— 用概率买掉维护成本,和快速排序随机选轴是同一门哲学。",
  },
  {
    type: "choice",
    q: "布隆过滤器的误判是单向的 —— 它可能犯哪种错误?",
    opts: [
      "假阳性:说「可能在」但其实不在;绝不假阴性 —— 它说「不在」就一定不在",
      "假阴性:可能漏报确实存在的元素",
      "两种错误都可能发生",
      "两种都不可能,它是精确的数据结构",
    ],
    correct: 0,
    wrong: [
      undefined,
      "插入时 K 个位全部置 1 且永不清零 —— 只要插入过,查询时这些位必然还是 1,不可能漏报。假阴性在结构上就不存在。",
      "只可能错一半:别的元素可以把你的位「顺手点亮」(造成假阳性),但没有任何操作能把已点亮的位熄灭(所以不会假阴性)。",
      "多个元素共享同一个位数组,不同词的哈希位可能重叠 —— 「全 1」不代表真的插入过。放弃精确性,正是它用来换取百倍内存节省的筹码。",
    ],
    why: "位只会从 0 变 1:查到「有 0」是铁证(一定没插入过);查到「全 1」只是嫌疑(可能是别人点亮的)。这个「一定不在 / 可能在」的单向承诺,恰好适合做第一道过滤门:放行的绝不冤枉,拦下的直接省掉一次昂贵查询。",
  },
  {
    type: "choice",
    q: "Redis 的有序集合 zset(数据量大时)底层用的是哪套组合?",
    opts: [
      "跳表(按分数有序、支持范围遍历)+ 哈希表(O(1) 按成员查分数)",
      "红黑树 + 哈希表",
      "有序数组 + 二分查找",
      "堆 + 哈希表",
    ],
    correct: 0,
    wrong: [
      undefined,
      "功能上红黑树也能胜任,但 Redis 作者选了跳表:实现简单得多、范围遍历直接沿底层链表顺序走、调试和改造都更友好。",
      "有序数组二分查得快,但 zset 的成员是频繁增删的 —— 数组插入删除 O(n) 搬家,扛不住。",
      "堆只保证堆顶是最值,内部整体无序,做不了「按排名取第 5 到第 15 名」这样的范围查询。",
    ],
    why: "zset = skiplist + dict 的双结构组合:跳表管「有序 + 范围查询」,字典管「O(1) 定位成员」—— 和 LRU 的「链表管顺序 + 哈希管定位」是同一个组合思想的亲兄弟。一次面试里能把这两个例子串起来讲,就是加分项。",
  },
];
