// 第 6 章 · 哈希表 —— 题单与测验数据。
// 题单围绕哈希三大信号(见过吗 / 配对 / 分组计数)+ 前缀和哈希 + 结构组合;
// hint 只给方向不剧透,key 用一段话把最优解讲透。

import type { Problem } from "@/lib/problems";
import type { QuizItem } from "@/lib/quiz";

export const PROBLEMS: Problem[] = [
  {
    lc: 217,
    title: "存在重复元素",
    d: "easy",
    tags: ["Set", "见过吗"],
    hint: "「有没有出现过」五个字,就是 Set 的岗位描述。一边遍历一边问。",
    key: (
      <>
        遍历数组,每个元素先问 Set「你见过它吗」:见过 → 立刻返回 true;
        没见过 → add 进 Set。O(n) 时间、O(n) 空间。对比排序解法 O(n log n):
        哈希用空间买时间,这笔交易是本章一切题目的底色。
      </>
    ),
  },
  {
    lc: 219,
    title: "存在重复元素 II",
    d: "easy",
    tags: ["Map", "最近下标"],
    hint: "不光要「见过」,还要「最近一次在哪见的」—— Set 升级成 Map。",
    key: (
      <>
        Map 存「值 → 最近一次出现的下标」。扫到 nums[i] 时若表里有它,
        且 i − 上次下标 ≤ k,返回 true;否则更新为最新下标(旧的更远,留着没用)。
        O(n)/O(n)。也可用「长度为 k 的滑动窗口 Set」,思路等价。
      </>
    ),
  },
  {
    lc: 383,
    title: "赎金信",
    d: "easy",
    tags: ["计数", "字符表"],
    hint: "magazine 里每个字母是「库存」,ransomNote 是「订单」—— 库存够不够?",
    key: (
      <>
        先遍历 magazine 给 26 个字母计数,再遍历 ransomNote 逐个扣减,
        扣成负数就是库存不足,返回 false。字符集固定时用长度 26
        的数组代替 HashMap,常数更小 —— 「计数数组是哈希表的青春版」。
        O(m + n) 时间、O(1) 空间(26 恒定)。
      </>
    ),
  },
  {
    lc: 290,
    title: "单词规律",
    d: "easy",
    tags: ["Map", "双向映射"],
    hint: "a→dog 存一张表还不够:dog→a 也得存 —— 想想为什么单向会漏判。",
    key: (
      <>
        建立字符 ↔ 单词的<b>双向</b>映射:char→word 和 word→char 两张表,
        任何一边冲突(a 已配 dog 又来配 cat;或 dog 已配 a 又来配 b)都返回 false。
        只存单向会放过 &quot;abba&quot; vs &quot;dog dog dog dog&quot; 这种多对一。
        与 LC 205(同构字符串)完全同型。
      </>
    ),
  },
  {
    lc: 202,
    title: "快乐数",
    d: "easy",
    tags: ["Set", "判环"],
    hint: "不快乐的数会怎样?它会绕圈。「绕圈」= 某个数第二次出现 = Set 的主场。",
    key: (
      <>
        反复做「各位平方和」变换,结果要么到 1(快乐),要么进入循环。
        用 Set 存出现过的中间值,一旦重现说明入环,返回 false。
        O(log n) 级别的每步计算,Set 保证检测环只花 O(1)。
        进阶:不用额外空间可上快慢指针(链表章的 Floyd 判环搬过来用)。
      </>
    ),
  },
  {
    lc: 349,
    title: "两个数组的交集",
    d: "easy",
    tags: ["Set", "去重"],
    hint: "把一个数组装进 Set,再拿另一个数组来「对暗号」。结果还要去重。",
    key: (
      <>
        nums1 全量入 Set A;遍历 nums2,凡在 A 中的加入结果 Set(自动去重),
        最后转数组。O(m + n)。Python 一行 <code>set(a) &amp; set(b)</code>、
        JS 用 <code>filter + has</code>、Java 用 retainAll —— 三语言的 Set
        都为「交集」这类集合运算而生。
      </>
    ),
  },
  {
    lc: 454,
    title: "四数相加 II",
    d: "medium",
    tags: ["Map", "分组配对"],
    hint: "四层循环 O(n⁴) 显然不行。四个数组拆成 2 + 2 呢?",
    key: (
      <>
        把 A+B+C+D=0 拆成「A+B 的和」与「−(C+D)」配对:先双层循环枚举 A、B,
        Map 记录每种和的出现次数(O(n²));再双层循环枚举 C、D,
        查 −(c+d) 在表中出现了几次,累加进答案。O(n²) 时间/空间,
        从 n⁴ 直落两个数量级 —— 「折半 + 哈希配对」是 meet in the middle
        思想的入门款。
      </>
    ),
  },
  {
    lc: 560,
    title: "和为 K 的子数组",
    d: "medium",
    tags: ["前缀和", "Map 计数", "必会"],
    hint: "子数组和 = 两个前缀和之差。「找一段区间」能不能变成「找一个之前出现过的前缀」?",
    key: (
      <>
        设前缀和 pre[i] = 前 i 个数之和,则子数组 (j, i] 的和为 k ⟺ pre[i] − pre[j] = k
        ⟺ <b>pre[j] = pre[i] − k</b>。于是一边扫一边维护 Map「前缀和 → 出现次数」:
        每到一个位置,先查 map[pre − k] 累加进答案(有几个这样的 j 就有几个合法子数组),
        再把当前 pre 的计数 +1。<b>初始必须放入 {"{0: 1}"}</b>——
        空前缀也是合法的 j,否则从下标 0 开始的子数组全部漏掉。O(n)/O(n)。
        注意元素有负数,滑动窗口失效(窗口和不单调),这正是前缀和 + 哈希登场的原因。
        它就是「两数之和」在前缀和世界里的翻版,是本题单的灵魂题。
      </>
    ),
  },
  {
    lc: 380,
    title: "O(1) 时间插入、删除和获取随机元素",
    d: "medium",
    tags: ["Map + 数组", "结构组合"],
    hint: "随机取要数组(下标随机),快速删要哈希(定位 O(1))—— 一个不够,就两个一起上。",
    key: (
      <>
        动态数组存值(支持 O(1) 随机取),Map 存「值 → 它在数组里的下标」。
        删除 x 时:查出 x 的下标 i,把<b>数组末尾元素搬到 i</b>、更新它在 Map
        里的下标,再弹掉末尾 —— 避开了数组中间删除的 O(n) 搬家。
        swap-with-last 是数组 O(1) 删除的标准戏法(代价是不保序)。
        这题是「组合结构」章的预告片:LRU、LFU 都是同款思路。
      </>
    ),
  },
  {
    lc: 299,
    title: "猜数字游戏",
    d: "medium",
    tags: ["计数", "两遍/一遍"],
    hint: "位置对 = 公牛,一眼能数。奶牛呢?两边各自「没配对上的数字」的库存交集。",
    key: (
      <>
        第一遍:同位且同值 → bulls++;不匹配的位置,分别给 secret 和 guess
        的数字计数(两个长度 10 的计数数组)。第二遍:每个数字 0–9,
        cows += min(cntS[d], cntG[d]) —— 库存能配上的部分都是奶牛。O(n)/O(1)。
        一遍流写法:一个计数数组,secret 的数字 +1、guess 的 −1,
        符号相反时说明抵了一头奶牛。
      </>
    ),
  },
];

export const QUIZ: QuizItem[] = [
  {
    type: "multi",
    q: "一个合格的哈希函数,必须满足以下哪些要求?(多选)",
    opts: [
      "确定性:同一个 key,今天算明天算结果必须相同",
      "均匀性:把 key 尽量平摊到所有桶,别扎堆",
      "要快:计算本身必须接近 O(1),否则「直达」就是空谈",
      "可逆性:必须能从哈希值反推出原始 key",
    ],
    correct: [0, 1, 2],
    missHint:
      "少选了:确定性(不然存进去就找不回来)、均匀(不然全挤一个桶退化成链表)、快(不然复杂度花在算哈希上)—— 三者缺一不可。",
    extraHint:
      "「可逆」恰恰是哈希不需要、甚至刻意避免的性质:多个 key 映射到同一个值本来就不可逆,密码哈希更是把不可逆当卖点。",
    why: "确定性保证「存得进、取得出」;均匀保证平均 O(1) 不退化;快保证省下来的时间没花在哈希计算上。可逆性不是要求 —— 哈希是多对一的压缩映射,天然不可逆。",
  },
  {
    type: "choice",
    q: "字符串哈希算出来可能是几十万的大数,为什么最后要对桶数取模(mod)?",
    opts: [
      "把任意大的哈希值压缩到 0 ~ 桶数−1,才能当数组下标用",
      "取模能让哈希分布变得绝对均匀",
      "为了加密,防止别人从桶位反推出 key",
      "为了让哈希值有序,方便之后二分查找",
    ],
    correct: 0,
    wrong: [
      undefined,
      "取模只是「压缩范围」,均匀与否主要取决于哈希函数本身;取模选得不好(比如桶数与 key 有公因子)反而会更不均匀。",
      "哈希表不负责保密 —— 取模是为了落进数组下标的合法范围,与加密无关。",
      "哈希表内部完全不排序,也不做二分 —— 它的一切都建立在「算出下标直达」上。",
    ],
    why: "桶数组本质是个数组,下标必须落在 [0, 桶数−1]。mod 就是那道「压缩闸门」—— 这也解释了扩容后为什么全部 key 都要重新安家:桶数变了,mod 的结果就变了。",
  },
  {
    type: "fill",
    q: (
      <>
        Java HashMap 默认在负载因子(load factor,已存元素数 ÷ 桶数)超过多少时触发扩容?
        (填一个小数)
      </>
    ),
    placeholder: "0.??",
    answers: ["0.75", ".75", "3/4"],
    hint: "在「桶太满冲突多」和「桶太空浪费内存」之间取的平衡点,是四分之三。",
    why: "0.75 是空间与时间的折中:负载再高,每个桶平均挂的元素变多,冲突链变长,O(1) 开始注水;负载再低,大片桶空着浪费内存。0.75 附近(数学上可用泊松分布证明)单桶长链的概率已经极小。",
  },
  {
    type: "choice",
    q: "哈希表平均 O(1),但最坏会退化到 O(n)。什么场景会触发最坏情况?",
    opts: [
      "所有 key 被哈希进同一个桶(哈希函数太差,或被恶意构造的 key 攻击)",
      "存的元素太多,超过一百万",
      "key 是字符串而不是整数",
      "没有按 key 的字典序插入",
    ],
    correct: 0,
    wrong: [
      undefined,
      "元素多不可怕 —— 扩容会把桶数同步撑大,只要分布均匀,每桶平均还是常数个。",
      "字符串哈希得当照样均匀;key 的类型不决定命运,分布才决定。",
      "哈希表根本不关心插入顺序 —— 它不维护任何顺序。",
    ],
    why: "全部 key 挤进一个桶时,哈希表实质退化成一条长链表,查找要逐个比对 O(n)。真实世界的 HashDoS 攻击就是构造大量同哈希的 key 拖垮服务器 —— Java 8 的树化(长链转红黑树,O(n)→O(log n))正是为此兜底。",
  },
  {
    type: "choice",
    q: "Java 里重写了 equals() 却忘记重写 hashCode(),把对象放进 HashMap 会发生什么?",
    opts: [
      "两个「相等」的对象可能算出不同哈希、落进不同的桶 —— 存进去却查不到",
      "编译直接报错,根本跑不起来",
      "没有任何影响,HashMap 只看 equals",
      "HashMap 会自动帮你按字段生成 hashCode",
    ],
    correct: 0,
    wrong: [
      undefined,
      "编译器不管这事(顶多 IDE 警告)—— 它是运行期的逻辑炸弹,更隐蔽也更危险。",
      "顺序恰恰相反:HashMap 先用 hashCode 找桶,找错了桶,equals 根本没机会出场。",
      "不会。默认 hashCode 基于对象内存身份,两个字段完全相同的 new 对象哈希值几乎必然不同。",
    ],
    why: "HashMap 的查找是两步:先 hashCode 定位桶,再在桶内用 equals 逐个比对。契约「equals 相等 ⇒ hashCode 必须相等」保证相等的对象进同一个桶。破坏契约 = 第一步就走错门,后面全白搭。",
  },
  {
    type: "choice",
    q: "Python 里 list 不能当 dict 的 key,tuple 却可以。根本原因是?",
    opts: [
      "list 可变,内容一变哈希值就该变,存进去就再也找不回来 —— 所以 list 干脆不实现 __hash__",
      "list 太长,算哈希太慢",
      "tuple 是 Python 的特权类型,语言开的后门",
      "list 里可能有 None,None 不能参与哈希",
    ],
    correct: 0,
    wrong: [
      undefined,
      "长短无关 —— 一个只有两个元素的 list 照样报 unhashable type。",
      "不是特权:tuple 能哈希是因为它不可变(且前提是内部元素也全部可哈希,包含 list 的 tuple 同样不行)。",
      "None 完全可以哈希,hash(None) 是个合法整数,它甚至能直接当 key。",
    ],
    why: "哈希表按「存入时的哈希值」决定住哪个桶。key 若在存入后被修改,重新计算的哈希指向别的桶,数据从此失联。Python 的对策釜底抽薪:可变容器(list/dict/set)一律不给 __hash__,把隐患挡在编译台前。",
  },
  {
    type: "choice",
    q: "JavaScript 里用普通 Object 当字典,和用 Map 相比,最要命的区别是?",
    opts: [
      "Object 的 key 会被强制转成字符串(obj[1] 和 obj['1'] 是同一个),Map 则任何类型都能当 key",
      "Object 存不下超过 100 个键值对",
      "Map 比 Object 慢十倍,只是写法好看",
      "两者完全等价,纯属风格问题",
    ],
    correct: 0,
    wrong: [
      undefined,
      "Object 没有这种容量限制 —— 问题在类型语义,不在数量。",
      "恰恰相反:频繁增删键的场景 Map 通常更快,引擎为它做了专门优化。",
      "远不等价:Object 还背着原型链(constructor、toString 是「幽灵 key」,in 运算符会误中),Map 则保证按插入顺序遍历、有 size 属性。",
    ],
    why: "Object 的 key 只能是 string 或 symbol,数字、对象都会被转成字符串;加上原型链污染风险(比如用户输入 __proto__ 当 key)。刷题与工程里,「字典」一律首选 Map/Set。",
  },
  {
    type: "choice",
    q: "Python 3.7+ 中,遍历 dict 的顺序是?",
    opts: [
      "保持插入顺序 —— 这是语言规范的正式承诺,不是巧合",
      "完全随机,每次运行都可能不同",
      "按 key 从小到大自动排序",
      "按哈希值大小排列",
    ],
    correct: 0,
    wrong: [
      undefined,
      "3.6 之前接近如此(且 3.3+ 对字符串 key 有随机盐)。3.7 起插入序写进了语言规范,可以放心依赖。",
      "dict 从不排序 —— 需要有序 key 得自己 sorted(d) 或用别的结构(对比 Java 的 TreeMap)。",
      "新版 CPython 的实现把「哈希定位」和「存储顺序」分离:紧凑数组按插入序存条目,哈希表只存下标 —— 所以遍历序与哈希值无关。",
    ],
    why: "CPython 3.6 用「紧凑 dict」实现顺带获得了插入序,3.7 把它升格为语言保证。对比:Java HashMap 无序(要顺序用 LinkedHashMap),JS Map 也保证插入序。「哈希表本身无序,具体实现可以额外附赠顺序」—— 这句话要分清。",
  },
];
