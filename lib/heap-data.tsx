// 第 9 章 · 堆与优先队列 —— 题单与测验数据。
// 题单主线:Top-K 门槛堆(703/973/692)→ 反复取最值(1046/767)→
// 合并 K 路(378)→ 双堆(295/502)。295 是本单的重点,key 里讲透。

import type { Problem } from "@/lib/problems";
import type { QuizItem } from "@/lib/quiz";

export const PROBLEMS: Problem[] = [
  {
    lc: 703,
    title: "数据流中的第 K 大元素",
    d: "easy",
    tags: ["Top-K", "容量 K 小根堆"],
    hint: "这就是 LC 215 的「数据流版」—— 那个容量 K 的小根堆,天生就是为流式数据设计的。",
    key: (
      <>
        构造时把初始数组塞进「容量 K 的小根堆」;每次 add:入堆,超过 K 就弹堆顶;
        返回堆顶即第 K 大。add 均摊 O(log K)、空间 O(K)。为什么全排序不行:
        数据流没有「全」—— 你永远不知道后面还来多少。Top-K 模板的最纯形态,
        堆里永远住着「迄今最大的 K 个」,堆顶是入围门槛。
      </>
    ),
  },
  {
    lc: 1046,
    title: "最后一块石头的重量",
    d: "easy",
    tags: ["大根堆", "模拟"],
    hint: "每回合都要「最重的两块」—— 哪个结构能随时供应最大值,还允许你不断塞回新值?",
    key: (
      <>
        全部入大根堆;循环:弹两次取最重的 x ≥ y,若 x ≠ y 把差值 x−y 塞回,
        直到堆里剩 ≤ 1 块。O(n log n)。Java 用{" "}
        <code>new PriorityQueue&lt;&gt;(Comparator.reverseOrder())</code>,
        Python 全程存负数 —— 这题正好把「大根堆技巧」练熟。
      </>
    ),
  },
  {
    lc: 973,
    title: "最接近原点的 K 个点",
    d: "medium",
    tags: ["Top-K", "容量 K 大根堆"],
    hint: "求「前 K 小」时,门槛应该是「当前 K 个里最远的」—— 这次轮到哪种堆守门?",
    key: (
      <>
        与 215 镜像:求前 K 小 → <b>容量 K 的大根堆</b>,堆顶是 K 个候选里最远的点,
        新点比它更近才有资格挤进来。O(n log K) 优于全排序 O(n log n),
        n 上亿、K 很小时是天壤之别。比距离用平方即可,不必开根号
        (单调函数不改变大小关系)。追问可提快速选择 O(n) 平均。
      </>
    ),
  },
  {
    lc: 692,
    title: "前 K 个高频单词",
    d: "medium",
    tags: ["堆 + 自定义排序", "哈希计数"],
    hint: "频次相同要按字典序 —— 比较器得同时说清两个维度,而且小根堆里「该淘汰的」要待在堆顶。",
    key: (
      <>
        哈希计数后建容量 K 的小根堆,比较器按「频次升序;频次相同按字典序<b>降序</b>」
        —— 反直觉的后半句是为了让「频次最低 + 字典序最大」这个最该被淘汰的组合
        恰好待在堆顶。最后逐个弹出、反转。O(n log K)。
        这题是「堆比较器方向感」的最佳练习:永远让堆顶住着最该被踢的元素。
      </>
    ),
  },
  {
    lc: 378,
    title: "有序矩阵中第 K 小的元素",
    d: "medium",
    tags: ["合并 K 路", "小根堆"],
    hint: "每一行都是一条升序序列 —— n 行矩阵就是 n 路归并,想想 LC 23。",
    key: (
      <>
        把每行行首 (值, 行, 列) 入小根堆;弹 k−1 次,每弹一个就把同行下一个元素补进来;
        第 k 次的堆顶即答案。O(k log n)。堆解法是「合并 K 路」模板的直接复用;
        面试加分:值域二分(猜答案 mid,O(n) 数出 ≤ mid 的个数)可做到
        O(n log(max−min)),两种都会才算吃透。
      </>
    ),
  },
  {
    lc: 767,
    title: "重构字符串",
    d: "medium",
    tags: ["贪心 + 大根堆", "计数"],
    hint: "每一步都贪心地用「当前剩余最多的字符」,但不能和上一个相同 —— 需要一个能动态报告最大值的结构。",
    key: (
      <>
        计数后按剩余次数入大根堆。每轮弹出剩最多的字符接到结果上,但它<b>先不回堆</b>
        (避免连用),等下一轮弹出新字符后,再把上一轮的(次数减一后若仍 &gt; 0)放回。
        若某字符数量 &gt; (n+1)/2 直接无解 —— 鸽笼原理,间隔都不够用。O(n log 26)。
        「反复取最值 + 取完还要动态更新」正是堆的主场。
      </>
    ),
  },
  {
    lc: 295,
    title: "数据流的中位数",
    d: "hard",
    tags: ["对顶双堆", "重点"],
    hint: "中位数把数据劈成两半:左半只关心「最大」,右半只关心「最小」—— 两个堆,各管一半。",
    key: (
      <>
        <b>对顶堆(本章压轴,务必讲透)</b>:大根堆 small 存较小的一半(堆顶 = 左半最大),
        小根堆 large 存较大的一半(堆顶 = 右半最小)。两条不变量:① small 的所有元素 ≤
        large 的所有元素;② 两堆大小差 ≤ 1(约定 small 可多 1)。addNum 的标准舞步:
        先把 x 压入 small,再把 small 堆顶搬去 large(这一步保证不变量 ①,
        因为搬走的是左半最大);若 large 反而更多,再搬一个回 small(保证 ②)。
        两次堆操作 O(log n),findMedian:奇数取 small 顶、偶数取两顶平均,O(1)。
        追问「99% 数据在 0~100」→ 计数数组 + 两端特殊桶,中位数扫 101 个桶即可。
      </>
    ),
  },
  {
    lc: 502,
    title: "IPO",
    d: "hard",
    tags: ["双堆接力", "贪心"],
    hint: "「本金够得着的项目里,选利润最大的」—— 两个条件、两把排序标准,一个堆管不过来。",
    key: (
      <>
        所有项目按<b>启动资本</b>入小根堆;做 k 轮:先把「当前资本够得着」的项目全部
        搬进按<b>利润</b>排序的大根堆(资本只增不减,搬进去就不用退回),
        再弹出利润最大的做掉、资本增加。每个项目两堆各进出一次:O(n log n)。
        「双堆接力 = 筛选门槛动态放宽」的贪心模板,和 295 的对顶堆是两种完全不同的双堆用法。
      </>
    ),
  },
];

export const QUIZ: QuizItem[] = [
  {
    type: "multi",
    q: "一个小根堆,能保证以下哪些事?(多选)",
    opts: [
      "堆顶是全场最小值",
      "每个父节点 ≤ 它的每个孩子",
      "左孩子 ≤ 右孩子",
      "底层数组从头到尾是升序的",
    ],
    correct: [0, 1],
    missHint:
      "堆的两条承诺:堆顶最值、每条父子链有序 —— 你漏掉了其中一条,再看看。",
    extraHint:
      "兄弟之间没有任何约束,底层数组也只是「堆序」不是「全序」—— 把 C 或 D 去掉。",
    why: "堆只维护「父 ≤ 子」这一件事(自然推出堆顶最小)。兄弟乱序、数组乱序都完全合法 —— 承诺越少,维护越便宜(log n 对全排序的 n log n),这正是堆的设计哲学。",
  },
  {
    type: "fill",
    q: "用数组存堆,下标 7 的节点,它父节点的下标是?",
    placeholder: "输入下标…",
    answers: ["3"],
    hint: "parent = (i − 1) / 2,整数除法向下取整:(7 − 1) / 2 = ?",
    why: "(7−1)/2 = 3;反向验证:3 的孩子是 2×3+1=7 和 2×3+2=8 ✓。完全二叉树按层编号没有空洞,所以父子关系不用指针,全靠这三条公式「算」出来 —— 和数组下标寻址一个思想。",
  },
  {
    type: "choice",
    q: "push / pop 的复杂度是 O(log n),根本原因是?",
    opts: [
      "上浮/下沉最多走一条根叶之间的路径,完全二叉树高度是 ⌊log₂n⌋",
      "每次操作要扫描一半的数组",
      "每次操作后要把堆重新排序",
      "其实是 O(1),因为只动堆顶或堆尾",
    ],
    correct: 0,
    wrong: [
      undefined,
      "上浮/下沉走的是一条「链」:每比较一次就上/下一层,不会横着扫一层的所有元素。",
      "堆从不做全排序 —— 它只修复被破坏的那一条父子链,其余部分原封不动。",
      "放进堆尾/摘掉堆顶本身确实 O(1),但之后必须上浮/下沉恢复堆序,路径长度是树高。",
    ],
    why: "完全二叉树 n 个节点高度 ⌊log₂n⌋,每步交换让元素上/下移一层,所以交换次数 ≤ 树高。堆的一切效率都来自「完全二叉树够矮」。",
  },
  {
    type: "choice",
    q: "求数组中「第 K 大」的元素,标准堆解法用哪种堆?",
    opts: [
      "容量 K 的小根堆 —— 堆顶是入围门槛,新数比门槛大才进",
      "容量 K 的大根堆",
      "全部元素入大根堆,连续弹 K 次",
      "全部元素入小根堆,连续弹 n−K 次",
    ],
    correct: 0,
    wrong: [
      undefined,
      "容量 K 的大根堆每次踢掉的是「K 个里最大的」,最后剩下的是 K 个最小值 —— 方向反了。求前 K 小才用大根堆。",
      "能做对,但所有元素入堆:空间 O(n)、时间 O(n + K log n),且数据流场景根本存不下全部 —— 容量 K 方案空间只要 O(K)。",
      "同样要把 n 个元素全部入堆,O(n) 空间;弹 n−K 次也比维护 K 容量贵。",
    ],
    why: "反直觉但精确:堆里常驻「迄今最大的 K 个」,想踢人时踢的是这 K 个里最小的 → 需要随时知道 K 个里的最小值 → 小根堆。堆顶 = 第 K 大候补 = 门槛。",
  },
  {
    type: "choice",
    q: "Python 的 heapq 只有小根堆,想要大根堆的惯用技巧是?",
    opts: [
      "所有数取负存入,弹出时再取负还原",
      "调用 heapq.maxheap() 切换模式",
      "给 heappush 传 reverse=True",
      "先 sort(reverse=True) 再当堆用",
    ],
    correct: 0,
    wrong: [
      undefined,
      "heapq 没有这个函数 —— 它是一组操作 list 的函数,不是类,更没有大根堆开关。",
      "reverse 是 sorted/sort 的参数,heappush 不认识它。",
      "排序是一次性的:新元素进来后有序性立刻失效,而且 O(n log n) 比堆贵。",
    ],
    why: "负号把「最大」映射成「最小」,小根堆照常工作。复合元素则给排序键取负打包成元组,如 (-freq, word);注意字符串不能取负 —— 那种情况要换思路(如自定义包装类或反转比较逻辑)。",
  },
  {
    type: "choice",
    q: "把 n 个已有元素建成堆,最优做法和复杂度是?",
    opts: [
      "从最后一个父节点倒着逐个下沉(Floyd 建堆),O(n)",
      "逐个 push 进空堆,O(n log n),这已是最优",
      "先排序再直接当堆用,O(n log n)",
      "无论怎么做都是 O(log n)",
    ],
    correct: 0,
    wrong: [
      undefined,
      "逐个 push 确实可行且是 O(n log n),但不是最优 —— 自底向上下沉能省一个 log。",
      "排好序的数组确实是合法堆,但排序本身 O(n log n),用牛刀杀鸡。",
      "光把 n 个元素各看一眼就要 O(n),总复杂度不可能低于 O(n)。",
    ],
    why: "一半节点是叶子(下沉 0 步)、1/4 最多沉 1 步、1/8 沉 2 步……「住得越低的人越多、搬家越近」,总步数 Σ(n/2^(d+1))·d 收敛到 O(n)。Python 的 heapq.heapify 就是它。",
  },
  {
    type: "choice",
    q: "Java 里 for (int x : priorityQueue) 遍历,输出顺序是?",
    opts: [
      "不保证任何有用的顺序 —— 想要有序只能连续 poll()",
      "从小到大(它毕竟叫优先队列)",
      "从大到小",
      "与插入顺序相同",
    ],
    correct: 0,
    wrong: [
      undefined,
      "迭代器走的是底层数组,数组只满足「堆序」:父子有序、兄弟叔侄之间可以任意乱 —— 打印 [1,3,2,7,4] 完全正常。",
      "同理,大根堆的数组同样只保证父子关系。",
      "元素入队时就经历了 siftUp 交换,插入顺序早被打乱了。",
    ],
    why: "堆的承诺只有「堆顶是最值」。需要有序序列:循环 poll()(每次 O(log n)),或者一开始就选错了结构 —— 该用 TreeMap / 排序。这是 PriorityQueue 在实战里翻车率最高的坑。",
  },
];
