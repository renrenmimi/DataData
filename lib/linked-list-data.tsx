// 第 3 章 · 链表 —— 题单与测验数据。
// 题单覆盖:删除/遍历、快慢指针、dummy 哨兵、反转、双链表综合,从 Easy 铺到 Hard;
// hint 只给方向不剧透,key 用一段话把最优解讲透。

import type { Problem } from "@/lib/problems";
import type { QuizItem } from "@/lib/quiz";

export const PROBLEMS: Problem[] = [
  {
    lc: 83,
    title: "删除排序链表中的重复元素",
    d: "easy",
    tags: ["遍历", "删除"],
    hint: "链表有序,重复的一定挤在一起。站在 cur 上,看看 cur.next 和自己是否相同。",
    key: (
      <>
        单指针一次遍历:若 cur.val == cur.next.val,就 cur.next = cur.next.next
        绕过去(注意此时 cur 不动 —— 后面可能还有连续重复);否则 cur 前进。
        头节点永远保留,所以这题不用 dummy。O(n) 时间、O(1) 空间。
      </>
    ),
  },
  {
    lc: 203,
    title: "移除链表元素",
    d: "easy",
    tags: ["dummy 哨兵", "删除"],
    hint: "要删的值可能出现在头节点 —— 这正是 dummy 哨兵的教科书场景。",
    key: (
      <>
        建 dummy 指向 head,cur 从 dummy 出发:cur.next.val == val 就
        cur.next = cur.next.next,否则前进。有了 dummy,「删头」和「删中间」
        是同一行代码 —— 和 §04 的对照示例一字不差。最后返回 dummy.next。
      </>
    ),
  },
  {
    lc: 876,
    title: "链表的中间结点",
    d: "easy",
    tags: ["快慢指针"],
    hint: "一次遍历怎么找中点?让两个人同时出发,一个人的速度是另一个人的两倍。",
    key: (
      <>
        快慢指针:slow 走 1 步、fast 走 2 步,fast 到尾时 slow 恰好在中点
        (偶数长度停在第二个中点,正合题意)。这是快慢指针最温和的用法,
        也是 LC 234 回文链表、148 排序链表的第一步。O(n)/O(1)。
      </>
    ),
  },
  {
    lc: 160,
    title: "相交链表",
    d: "easy",
    tags: ["双指针", "路径互换"],
    hint: "两条链长度不同没法对齐?让每个指针走完自己的路,再去走对方的路。",
    key: (
      <>
        双指针 pA、pB 各从一条链出发,到尾后跳到<b>另一条</b>链的头继续。
        两人走的总路程都是 a+b,所以要么在交点相遇,要么同时到达
        null(不相交)。不用算长度、不用哈希表,优雅到值得背下来。O(n+m)/O(1)。
      </>
    ),
  },
  {
    lc: 234,
    title: "回文链表",
    d: "easy",
    tags: ["快慢指针", "反转", "综合"],
    hint: "链表没法从尾往头走 —— 但你已经会「找中点」和「反转」两个零件了,拼起来?",
    key: (
      <>
        三步组合拳:快慢指针找中点 → 反转后半段 → 双指针从两端(头和原尾)
        同步比较。O(n) 时间、O(1) 空间,比「拷进数组再对撞」省掉 O(n) 空间。
        这题是本章零件的第一次总装,面试出镜率极高。
      </>
    ),
  },
  {
    lc: 19,
    title: "删除链表的倒数第 N 个结点",
    d: "medium",
    tags: ["双指针", "间隔同步", "dummy 哨兵"],
    hint: "让 fast 先走 n 步,再和 slow 一起走 —— fast 到尾时,slow 在哪?",
    key: (
      <>
        fast 从 dummy 先走 n+1 步,slow 从 dummy 同步出发,fast 到 null 时
        slow 恰好停在<b>待删节点的前驱</b>上,一行 slow.next = slow.next.next
        完成删除。dummy 兜住「删的是头节点」的情况。一次遍历 O(n)。
      </>
    ),
  },
  {
    lc: 24,
    title: "两两交换链表中的节点",
    d: "medium",
    tags: ["dummy 哨兵", "指针操作"],
    hint: "交换一对节点要改三根指针。谁站在这一对的前面?dummy 又立功了。",
    key: (
      <>
        dummy 后挂 head,prev 每次盯着一对 (a, b):prev.next = b、a.next =
        b.next、b.next = a,然后 prev 跳到 a。画图确认三根指针的顺序 ——
        先记住 b.next 再动手,和「先接后断」同理。递归版更短,但迭代版空间 O(1)。
      </>
    ),
  },
  {
    lc: 142,
    title: "环形链表 II",
    d: "medium",
    tags: ["快慢指针", "数学推导"],
    hint: "LC 141 只问有没有环,这题要找入口。相遇点不是入口,但相遇后再做一次同速行走……",
    key: (
      <>
        Floyd 判圈第二阶段:快慢相遇后,把一个指针放回 head,两人<b>同速</b>前进,
        再次相遇处就是环入口。推导:设头到入口 a、入口到相遇点 b,相遇时
        fast 路程 = 2×slow 路程 → a = c(相遇点绕回入口的距离)+ 若干整圈。
        面试常要求写出这条等式,§06 精讲 B 的证明直觉是基础。
      </>
    ),
  },
  {
    lc: 2,
    title: "两数相加",
    d: "medium",
    tags: ["dummy 哨兵", "模拟", "进位"],
    hint: "数字按低位在前存好了 —— 这正是加法竖式的顺序。逐位加,别忘了进位这个「隐藏的第三个加数」。",
    key: (
      <>
        dummy + tail 建结果链:每步 sum = a + b + carry(链短的按 0 算),
        挂上 sum % 10,carry = sum / 10。循环条件是「l1 或 l2 或 carry
        还有货」—— 最后一个进位漏挂是这题的第一大 WA 点。O(max(n,m))。
      </>
    ),
  },
  {
    lc: 92,
    title: "反转链表 II",
    d: "medium",
    tags: ["反转", "头插法", "dummy 哨兵"],
    hint: "只反转 [left, right] 区间:先走到区间前一个节点,然后把区间里的节点一个个「头插」到它后面。",
    key: (
      <>
        dummy 定位 pre(left 的前驱)后用头插法:每次把 cur.next 摘下来,
        插到 pre 后面,重复 right−left 次 —— 区间自然反转,且只遍历一次。
        比「切段 + 反转 + 缝回」少一半指针操作,但每一步都建议画图,
        这题是指针操作的期末考。O(n)/O(1)。
      </>
    ),
  },
  {
    lc: 25,
    title: "K 个一组翻转链表",
    d: "hard",
    tags: ["反转", "分组", "dummy 哨兵"],
    hint: "把 LC 206(整段反转)封装成函数,再按 K 个一段调用它 —— 难点全在段与段的「缝合」。",
    key: (
      <>
        dummy 起手,每轮:① 探查剩余是否够 K 个(不够则结束,保持原序);
        ② 反转这 K 个(206 的三指针);③ 缝合 —— 上一段的尾接新段的头、
        新段的尾接下一段的头。用 groupPrev / groupNext 两个锚点变量能少掉
        大半混乱。它 Hard 在工程化拆解,而不是新算法 —— 能在面试中完整写对,说明链表的指针操作已经掌握。
      </>
    ),
  },
];

export const QUIZ: QuizItem[] = [
  {
    type: "choice",
    q: "「链表插入/删除是 O(1)」这句话成立的完整前提是?",
    opts: [
      "已经持有插入/删除位置的前驱节点引用 —— 找到这个前驱本身可能要 O(n)",
      "无条件成立,链表插删就是比数组快",
      "链表必须是有序的",
      "只在链表长度小于 100 时成立",
    ],
    correct: 0,
    wrong: [
      undefined,
      "「按位置 i 插入」全程其实是 O(n):走到 i-1 花 O(n),改指针才是 O(1)。快的只是“改指针”这一步。",
      "有序与否不影响插删成本,影响的是查找方式(而且链表就算有序也没法二分 —— 没有随机访问)。",
      "复杂度描述的是随规模增长的趋势,与具体数值无关。",
    ],
    why: "链表插删 = 找前驱 + 改指针。改指针恒 O(1),找前驱一般 O(n)。所以链表真正的优势场景是「本来就拿着节点引用」的情况 —— 比如 LRU 缓存里,哈希表直接递给你节点,删除才是真 O(1)。",
  },
  {
    type: "choice",
    q: "在 prev 和 cur 之间插入新节点 node,两步指针操作的正确顺序是?",
    opts: [
      "先 node.next = cur(先接),再 prev.next = node(后断)",
      "先 prev.next = node,再 node.next = cur",
      "两步顺序无所谓,结果一样",
      "只需要 prev.next = node 一步",
    ],
    correct: 0,
    wrong: [
      undefined,
      "prev.next 一旦改指向 node,cur 及其后面整条链就没人记得了 —— 除非你提前备份。默认写法必须先接后断。",
      "顺序恰恰是生死攸关的:反过来会弄丢 cur 开始的整个后半段(LinkedLab 里的反面教材演示的就是它)。",
      "只改 prev.next 的话,node.next 还是悬空的 —— 链在 node 处断头,后半段全丢。",
    ],
    why: "口诀「先接后断」:新节点先牵住后继(node.next = cur),原链没有任何损失;然后 prev 才改挂新节点。顺序反了,cur 起的整条后半链失去所有引用 —— 数据丢失。",
  },
  {
    type: "choice",
    q: "快慢指针判环:为什么有环时两者「一定」相遇,而不会互相跳过?",
    opts: [
      "进环后 fast 每步比 slow 多走 1 格,两者距离每步恰好缩小 1,必然减到 0",
      "因为环的长度一定是偶数",
      "运气好才相遇,算法有小概率失败",
      "因为 slow 会停下来等 fast",
    ],
    correct: 0,
    wrong: [
      undefined,
      "环长任意(奇偶都行)。关键在相对速度是 1:一步一步逼近,永远不会“隔着 1 格互相穿过”。",
      "这是确定性算法:距离严格单调递减到 0,没有任何概率成分。",
      "两个指针每轮都在动。相遇靠的是速度差,不是谁等谁。",
    ],
    why: "把 slow 看作静止参照系:fast 以每步 1 格的相对速度追它。距离是整数、每步减 1,减到 0 就是相遇 —— 若 fast 每步走 3(相对速度 2),距离为奇数时才可能跳过,这就是「快 2 倍」而不是快 3 倍的原因之一。",
  },
  {
    type: "multi",
    q: "以下哪些场景应该优先选「数组」而不是链表?(多选)",
    opts: [
      "需要大量按下标随机访问",
      "顺序遍历的性能敏感(想吃 CPU 缓存红利)",
      "手里拿着节点引用,要在它旁边频繁插入/删除",
      "元素总量基本固定,很少插删",
    ],
    correct: [0, 1, 3],
    missHint: "数组的三大主场:下标直达、连续内存(缓存友好)、规模稳定。对照选项再数一遍,你漏了哪个?",
    extraHint: "「拿着节点引用原地插删」恰恰是链表唯一碾压数组的主场 —— 数组做这事要 O(n) 搬家,不能选它。",
    why: "数组连续存放:下标 O(1) + 缓存命中率高 + 不插删就没有搬家成本。链表赢在「已知位置的 O(1) 插删」和「不需要连续大块内存」。第 1 章讲过:实际工程里数组遍历常比链表快一个数量级,就是缓存行的功劳。",
  },
  {
    type: "fill",
    q: (
      <>
        Java 的 <code>LinkedList</code> 是双向链表,那么{" "}
        <code>list.get(i)</code> 的时间复杂度是?(用 O(…) 作答)
      </>
    ),
    placeholder: "输入复杂度,如 O(1)…",
    answers: ["O(n)", "o(n)", "On"],
    hint: "链表没有下标公式 —— get(i) 只能从头(或尾,JDK 会挑近的一端)一个 next 一个 next 地走过去。",
    why: "LinkedList.get(i) 是 O(n):它要逐节点走到 i。经典事故:用 for (int i = 0; i < list.size(); i++) list.get(i) 遍历 LinkedList,总复杂度 O(n²)。遍历链表要用 for-each 或迭代器 —— 或者一开始就用 ArrayList。",
  },
  {
    type: "choice",
    q: "dummy(哑结点/哨兵)技巧的核心作用是?",
    opts: [
      "让头节点变成「普通节点」:所有操作统一成“改前驱的 next”,消灭头部特判",
      "提升链表的访问速度",
      "省内存",
      "防止链表成环",
    ],
    correct: 0,
    wrong: [
      undefined,
      "dummy 不改变任何复杂度 —— 它优化的是「代码形状」:少一个 if,少一处 bug。",
      "恰恰要多花一个节点的内存 —— 用一个节点换掉一类边界 bug,血赚。",
      "dummy 和环没有关系;判环靠快慢指针。",
    ],
    why: "头节点特殊,是因为它没有前驱 —— 删除/插入它要单独写一支代码。dummy 站到 head 前面,人人都有前驱,一套「cur.next 操作」通吃,最后返回 dummy.next。LC 203、19、21、25 全靠它简化。",
  },
  {
    type: "choice",
    q: "找链表中点有两种做法:①先遍历数长度 n,再走 n/2 步;②快慢指针一次遍历。哪个说法正确?",
    opts: [
      "两种都是 O(n);快慢指针的优势是只扫一遍,适合流式数据或想少写一个循环的场合",
      "快慢指针是 O(log n),数长度是 O(n)",
      "数长度法是错的,得不到中点",
      "快慢指针总步数更少,是数长度法的一半",
    ],
    correct: 0,
    wrong: [
      undefined,
      "快慢指针没有“砍一半”的结构:slow 走 n/2 步、fast 走 n 步,还是线性 —— 链表上不存在 O(log n) 的定位。",
      "数长度法完全正确,两次循环各 O(n),写起来甚至更不容易错 —— 只是要遍历两遍。",
      "算总账:快慢指针 n/2 + n = 1.5n 步,数长度 n + n/2 = 1.5n 步 —— 打平。差别在遍历「遍数」,不在步数。",
    ],
    why: "两种都对、都是 O(n),总步数还恰好都是 1.5n。快慢指针赢在单次扫描(数据只能读一遍的流式场景是刚需),且和判环、回文链表共享同一套模板 —— 这就是它成为面试默认解的原因。",
  },
];
