// 第 5 章 · 队列与双端队列 —— 题单与测验数据。
// 题单从模拟铺到「前缀和 + 单调队列」Hard;hint 只给方向,key 一段话讲透最优解。
// 注:原题单中的 LC 346(数据流中的移动平均值)为会员题,已替换为 641 / 946。

import type { Problem } from "@/lib/problems";
import type { QuizItem } from "@/lib/quiz";

export const PROBLEMS: Problem[] = [
  {
    lc: 933,
    title: "最近的请求次数",
    d: "easy",
    tags: ["队列", "时间窗口"],
    hint: "只关心最近 3000ms 内的请求 —— 过期的请求从哪一端离开?新请求从哪一端进来?",
    key: (
      <>
        新请求入队(队尾);然后把时间戳 &lt; t − 3000 的队头挨个出队;
        队列长度就是答案。时间单调递增 → 过期的一定聚在队头,
        这是「时间滑动窗口」的最小模型。每个请求进出队各一次,均摊 O(1)。
      </>
    ),
  },
  {
    lc: 225,
    title: "用队列实现栈",
    d: "easy",
    tags: ["队列", "旋转"],
    hint: "新元素入队后排在队尾 —— 怎么让它「转」到队头,变成下一个出队的?",
    key: (
      <>
        push 时先把新元素入队,再把它前面的 n−1 个元素依次出队、重新入队:
        队列旋转一圈,新元素恰好转到队头 → pop 就是普通出队。push O(n)、pop
        O(1)。与本章精讲 A(LC 232)互为镜像,一起做最有感觉。
      </>
    ),
  },
  {
    lc: 622,
    title: "设计循环队列",
    d: "medium",
    tags: ["循环队列", "取模"],
    hint: "本章 §04 的手写实现就是这道题 —— 先自己写,卡住了再回去看。",
    key: (
      <>
        定长数组 + front/rear 两个指针,前进一律 <code>(i + 1) % cap</code>{" "}
        绕圈。满/空判定二选一:留一格空(开 k+1 格,(rear+1)%cap==front 即满)
        或维护 size 计数器。所有操作 O(1),零搬家 —— RingLab 玩过的一切,
        在这里变成代码。
      </>
    ),
  },
  {
    lc: 641,
    title: "设计循环双端队列",
    d: "medium",
    tags: ["循环队列", "deque"],
    hint: "622 的加强版:两端都要能进出。front 往「后退」一步的下标怎么算才不会变成负数?",
    key: (
      <>
        在 622 基础上补两个方向:队头插入 = front 先退一步再写,队尾删除 =
        rear 先退一步。后退的标准写法是 <code>(i − 1 + cap) % cap</code> ——
        先加 cap 再取模,避免负数(多数语言的 % 对负数不友好)。
        写完它,就等于自行实现了 ArrayDeque 的核心逻辑。
      </>
    ),
  },
  {
    lc: 946,
    title: "验证栈序列",
    d: "medium",
    tags: ["栈", "模拟", "贪心"],
    hint: "别推理,直接演:按 pushed 顺序真的往栈里压,能弹就弹,看最后能不能演完。",
    key: (
      <>
        贪心模拟:按 pushed 依次入栈;每次入栈后,只要栈顶 == popped
        的当前元素就一直弹并推进 popped 指针。结束时栈空 ⇔ 序列合法。
        为什么贪心是对的?能弹却不弹,那个元素之后会被压得更深,永远错过。
        O(n)/O(n),上一章栈的完美复习题。
      </>
    ),
  },
  {
    lc: 649,
    title: "Dota2 参议院",
    d: "medium",
    tags: ["队列", "贪心", "循环处理"],
    hint: "每个议员的最优策略是禁掉「下一个即将行动的对手」。回合制 + 先来先行动 → 用什么结构排班?",
    key: (
      <>
        两个队列分别存 R、D 议员的<b>下标</b>:每轮取两队队头比较,
        下标小者先行动 → 存活,并以「下标 + n」重新入队(表示下一轮再来,
        循环!);下标大者被禁言,直接出队消失。直到一方清空。
        队列天然维护「下一个行动者」的顺序,O(n)。
      </>
    ),
  },
  {
    lc: 1438,
    title: "绝对差不超过限制的最长连续子数组",
    d: "medium",
    tags: ["滑动窗口", "双单调队列"],
    hint: "窗口合法 ⇔ 窗口内 max − min ≤ limit。滑窗最大值用递减队列 —— 最小值呢?再开一个。",
    key: (
      <>
        滑动窗口 + 两个单调队列:递减队列随时给出窗口 max,递增队列给出窗口
        min。右端扩张后,若 max − min &gt; limit 就收缩左端,同时把过期下标
        从两个队头弹掉。LC 239 的直接升级:一个窗口,两套单调性。O(n)。
      </>
    ),
  },
  {
    lc: 862,
    title: "和至少为 K 的最短子数组",
    d: "hard",
    tags: ["前缀和", "单调队列"],
    hint: "有负数,普通滑窗失效(窗口和不单调)。先转成前缀和:找 P[r] − P[l] ≥ K 且 r − l 最短。",
    key: (
      <>
        对前缀和 P 维护<b>递增</b>单调队列:遍历 r 时,①队头满足 P[r] −
        P[队头] ≥ K 就结算并<b>弹出队头</b>(它已配对到最短,留着只会更长);
        ②队尾 ≥ P[r] 的全部弹掉(比 P[r] 大又比它靠左,不可能再成为最大值)。
        两条弹出规则正好用到 deque 两端 —— 这就是它必须是双端队列的原因。O(n)。
      </>
    ),
  },
];

export const QUIZ: QuizItem[] = [
  {
    type: "choice",
    q: "队列的 FIFO(First In, First Out)指的是?",
    opts: [
      "最先入队的最先出队 —— 队尾进、队头出,两端各司其职",
      "最后入队的最先出队",
      "元素按值从小到大出队",
      "进和出都发生在同一端",
    ],
    correct: 0,
    wrong: [
      undefined,
      "后进先出(LIFO)是栈(上一章)—— 队列讲究公平:先来的先被服务。",
      "按值出场的是优先队列/堆(第 9 章);普通队列只看「谁先来」,不看大小。",
      "同一端进出是栈的设计;队列开两个口:rear 只进,front 只出。",
    ],
    why: "队列 = 排队买奶茶:新人从队尾(rear)加入,店员只服务队头(front)。进出分居两端,这是它与栈唯一、也是根本的区别 —— 由此才有「公平」这个性质。",
  },
  {
    type: "choice",
    q: "循环队列入队时执行 rear = (rear + 1) % capacity,取模是为了?",
    opts: [
      "让下标越过数组末尾时绕回 0,复用出队腾出的格子 —— 不搬家也不浪费",
      "把元素打散均匀分布,防止哈希冲突",
      "把下标变成随机数,更安全",
      "取模运算比加法更快",
    ],
    correct: 0,
    wrong: [
      undefined,
      "哈希冲突是哈希表(第 6 章)的词 —— 这里取模只是把「一条直线」在逻辑上折成「一个圈」,元素位置完全确定。",
      "取模结果毫无随机性:下标 7 的下一格永远是 0,可预测才谈得上是队列。",
      "取模其实比加法慢 —— 用它买的是语义(绕圈复用),不是速度。",
    ],
    why: "数组物理上是直的,% 让下标走到尽头就回 0:front 出队腾出的旧格子,rear 绕一圈回来复用。于是出队只挪指针 O(1)、空间零浪费 —— 一个取模同时解决朴素数组队列的两个坑。",
  },
  {
    type: "choice",
    q: "循环队列中 front == rear 时,队列是满还是空?",
    opts: [
      "分不清 —— 所以要么留一格空(满 = rear+1 追上 front),要么额外维护 size 计数器",
      "一定是空",
      "一定是满",
      "这种状态不可能出现",
    ],
    correct: 0,
    wrong: [
      undefined,
      "刚创建时 front == rear 确实是空;但只进不出让 rear 绕整整一圈,追上 front 的那一刻是满 —— 同一个指针状态,两种含义。",
      "反例更简单:刚 new 出来的队列 front == rear,明明一个元素都没有。",
      "空和满都会走到 front == rear —— 它不但可能出现,还是循环队列设计里最核心的歧义,必须显式消解。",
    ],
    why: "空:front 追上 rear;满:rear 绕一圈追上 front —— 指针状态完全相同。方案 A 留一格空:牺牲一格换零额外变量;方案 B 计数器:8 格全能用,代价是每次进出多维护一个 size。RingLab 里可以来回切换体验。",
  },
  {
    type: "choice",
    q: "双端队列 deque 在两端插入 / 删除的复杂度是?",
    opts: [
      "两端都是 O(1)(循环数组或块状链表实现),但随机访问中间不是它的强项",
      "队头 O(1),队尾 O(n)",
      "两端都是 O(log n)",
      "和普通数组一样:头部 O(n)",
    ],
    correct: 0,
    wrong: [
      undefined,
      "deque 的意义就在两端平权:Java ArrayDeque 用循环数组、Python deque 用块状双向链表 —— 哪端操作都不用搬家。",
      "O(log n) 来自树形结构「每步砍一半」;deque 两端操作只挪指针/写一格,是纯 O(1)。",
      "「头部 O(n)」正是朴素数组的搬家成本 —— deque 存在的意义就是消灭它。",
    ],
    why: "deque(double-ended queue)两端全开:头尾进出全 O(1)。只用一端 = 栈,两端各用一个 = 队列 —— 一个容器分饰两角,这就是 Java/Python 刷题都拿它当万能容器的原因。",
  },
  {
    type: "multi",
    q: "在 JavaScript 里刷题需要队列,哪些做法能保住出队 O(1)?(多选)",
    opts: [
      "双栈模拟:in 收 push,out 管 pop,均摊 O(1)",
      "手写链表队列:head 出、tail 进",
      "下标假装出队:维护 head 指针只前移,不真删元素",
      "直接用 Array.shift()",
    ],
    correct: [0, 1, 2],
    missHint:
      "A、B、C 三种做法都绕开了「头部删除要搬家」—— 想想各自是怎么绕开的,再补上漏掉的那个。",
    extraHint:
      "D 正是那个大坑:shift() 抽走头部后,剩余所有元素集体前移一格,O(n) —— 数据一大必超时。",
    why: "JS 标准库没有真正的队列:shift() 每次 O(n)。双栈靠均摊、链表靠指针摘除、下标法靠「假装删除」,都把出队做到了 O(1)。刷题首选下标法 —— 三行代码,零心智负担。",
  },
  {
    type: "choice",
    q: "单调队列(以滑动窗口最大值 LC 239 为例)在队列里维护的是什么?",
    opts: [
      "一段值递减的「候选」下标:队头永远是当前窗口最大值;新元素入队前把队尾比它小的全部弹出",
      "窗口内的所有元素,一个不漏",
      "窗口内元素排好序的副本(从小到大)",
      "只存当前窗口的最大值这一个数",
    ],
    correct: 0,
    wrong: [
      undefined,
      "全都留下就退化成普通队列,查最大值还得 O(k) 扫描 —— 单调队列的精髓恰恰是大胆丢弃「永远当不上最大」的元素。",
      "维护完整有序副本,插入删除至少 O(log k)(那是平衡树/堆的做法);单调队列只靠两端 O(1) 操作。",
      "只存一个数,等最大值滑出窗口(过期)时,就找不到「第二名」接班了 —— 必须保留整条递减候选链。",
    ],
    why: "两条规则:①比我小又比我早 → 不可能再成为最大值 → 从队尾弹出;②队头滑出窗口(过期)→ 从队头弹掉。一端弹出、一端过期,所以必须是双端队列 —— 队头始终是在任最大值,取答案 O(1)。",
  },
  {
    type: "fill",
    q: (
      <>
        双栈模拟队列(LC 232)中,每个元素一生最多被操作 4 次(进
        in、出 in、进 out、出 out),所以出队的均摊复杂度是 O(___)?
      </>
    ),
    placeholder: "填复杂度…",
    answers: ["1", "O(1)", "o(1)", "(1)", "常数"],
    hint: "把「≤ 4n 次总操作」摊到 n 次调用上,平均每次是多少?",
    why: "单次 pop 偶尔要把 in 整体转移进 out(那一下 O(n)),但每个元素一生只被倒一次 —— 总账 ≤ 4n 次操作 ÷ n 次调用 = 常数,均摊 O(1)。和数组扩容、单调栈同一招:看总账,不盯最贵的那一次。",
  },
];
