// 第 4 章 · 栈 —— 题单与测验数据。
// 题单从配对消除铺到单调栈 Hard;hint 只给方向不剧透,key 一段话讲透最优解。

import type { Problem } from "@/lib/problems";
import type { QuizItem } from "@/lib/quiz";

export const PROBLEMS: Problem[] = [
  {
    lc: 1047,
    title: "删除字符串中的所有相邻重复项",
    d: "easy",
    tags: ["栈", "配对消除"],
    hint: "像玩消消乐:新字符和「最近留下的那个」相同就一起消失。「最近的」三个字,该想到谁?",
    key: (
      <>
        逐字符入栈:当前字符与栈顶相同 → 弹栈(这一对消除),否则入栈;
        扫完后栈里剩下的就是答案。妙处在于消除后「新栈顶」自动暴露出来,
        能和后面的字符继续配对 —— 连锁消除不需要任何额外处理。O(n)/O(n)。
      </>
    ),
  },
  {
    lc: 232,
    title: "用栈实现队列",
    d: "easy",
    tags: ["双栈", "均摊分析"],
    hint: "一个栈会把顺序反过来 —— 那再反一次呢?",
    key: (
      <>
        in 栈只收 push,out 栈只管 pop/peek;out 空了才把 in 整体倒过去。
        两次反转 = 恢复先来后到。每个元素一生最多被搬 4 次(进出 in、进出 out),
        所以均摊 O(1)。队列章精讲 A 有逐帧动画与完整均摊证明。
      </>
    ),
  },
  {
    lc: 225,
    title: "用队列实现栈",
    d: "easy",
    tags: ["队列", "旋转"],
    hint: "新元素入队后排在最后 —— 有没有办法让它「转」到队头去?",
    key: (
      <>
        单队列做法:push 时先把新元素入队,再把它前面的 n−1 个元素依次出队、
        重新入队 —— 队列被「旋转」一圈,新元素恰好转到队头。于是 pop = 出队,
        push O(n)、pop O(1)。和 LC 232 互为镜像,放在一起做最有感觉。
      </>
    ),
  },
  {
    lc: 682,
    title: "棒球比赛",
    d: "easy",
    tags: ["栈", "模拟"],
    hint: "每条指令都只关心「最近的一两次有效得分」—— 用什么存最近的记录?",
    key: (
      <>
        用栈存有效得分:数字直接入栈;「+」取栈顶两项之和入栈;「D」把栈顶
        ×2 入栈;「C」弹掉栈顶。最后求和。纯模拟题,练的是「最近记录 → 栈」
        的条件反射。O(n)/O(n)。
      </>
    ),
  },
  {
    lc: 150,
    title: "逆波兰表达式求值",
    d: "medium",
    tags: ["栈", "表达式求值"],
    hint: "后缀表达式里,运算符永远跟在它的两个操作数后面 —— 「最近的两个数」在哪里等你?",
    key: (
      <>
        数字入栈;遇到运算符弹出两个数,算完把结果压回去。注意弹出顺序:
        <b>先弹出的是右操作数</b>(对减法/除法致命),以及除法要向零取整。
        逆波兰式不需要括号、不需要优先级 —— 这正是编译器/计算器用栈求值的方式,
        LC 20 + 150 合起来就是一台迷你解释器。O(n)/O(n)。
      </>
    ),
  },
  {
    lc: 394,
    title: "字符串解码",
    d: "medium",
    tags: ["栈", "嵌套结构"],
    hint: "3[a2[c]] —— 嵌套的括号,里层必须先解码。「里层优先」等价于哪个词?",
    key: (
      <>
        遇到 <code>[</code> 时把「当前已拼好的字符串 + 当前倍数」打包压栈,
        然后清零重新开始;遇到 <code>]</code> 弹栈,cur = prev + k×cur。
        栈里存的是「每一层未完成的半成品」,和调用栈保存断点一模一样 ——
        事实上本题用递归写也行,两种写法互为翻译。O(n)。
      </>
    ),
  },
  {
    lc: 496,
    title: "下一个更大元素 I",
    d: "easy",
    tags: ["单调栈", "哈希表"],
    hint: "先对 nums2 把每个元素的「下一个更大」全求出来存好,再回答 nums1 的提问。",
    key: (
      <>
        对 nums2 跑一遍单调递减栈:新元素比栈顶大就弹栈,并用哈希表记录
        「被弹者 → 弹它的人」;最后 nums1 逐个查表。单调栈打表 + 哈希查询,
        O(n + m)。这是 739 的孪生题,先做它找手感。
      </>
    ),
  },
  {
    lc: 503,
    title: "下一个更大元素 II",
    d: "medium",
    tags: ["单调栈", "循环数组"],
    hint: "数组是环形的,最后一个元素的「下一个」可能绕回开头 —— 把数组「虚拟地」拉长成两倍试试。",
    key: (
      <>
        循环数组标准技巧:下标 i 从 0 跑到 2n−1,访问时用 <code>i % n</code>;
        第一圈正常「弹栈结算 + 入栈」,第二圈<b>只弹不进</b>(它们的答案第一圈已定,
        再入栈会重复)。不用真的复制数组,一个取模全搞定。O(n)。
      </>
    ),
  },
  {
    lc: 84,
    title: "柱状图中最大的矩形",
    d: "hard",
    tags: ["单调栈", "哨兵"],
    hint: "以某根柱子为「高」的最大矩形,取决于它左右两边第一根「比它矮」的柱子 —— 两个方向的「下一个更小」。",
    key: (
      <>
        维护<b>递增</b>栈:当前柱比栈顶矮时,弹出栈顶并结算「以它为高」的矩形
        —— 右边界是当前下标,左边界是弹出后的新栈顶,宽 = i − 新栈顶 − 1。
        首尾各加一根高度 0 的哨兵柱,开头免判空、结尾强制清算,边界代码全省。
        单调栈的天花板题,吃透它 42/739/496 全部通杀。O(n)。
      </>
    ),
  },
  {
    lc: 42,
    title: "接雨水",
    d: "hard",
    tags: ["单调栈", "一题两解"],
    hint: "数组章用对撞指针「竖着」按列算过它 —— 单调栈可以「横着」按层算:凹槽的底被弹出时,一层水就能结算。",
    key: (
      <>
        维护递减栈(存下标):当前柱比栈顶高 → 栈顶是「凹槽底」,弹出它;
        此时左墙 = 新栈顶、右墙 = 当前柱,这一层水 = (min(左墙,右墙) − 底)×
        (右墙下标 − 左墙下标 − 1)。与数组章的对撞指针解法(按列竖算)对照:
        同一道题,「竖切」是双指针的世界观,「横切」是单调栈的世界观 ——
        面试能讲出两种,才算真正吃透。O(n)。
      </>
    ),
  },
];

export const QUIZ: QuizItem[] = [
  {
    type: "choice",
    q: "栈的 LIFO(Last In, First Out)指的是?",
    opts: [
      "最后入栈的元素最先出栈 —— 进和出都发生在同一端(栈顶)",
      "最先入栈的元素最先出栈",
      "元素按值从大到小出栈",
      "入栈和出栈分别在两端进行",
    ],
    correct: 0,
    wrong: [
      undefined,
      "先进先出(FIFO)是队列(下一章)的规矩 —— 栈恰好相反:后来者居上,先被处理。",
      "栈不关心元素大小,只关心「谁最后进来」;按值出场的是堆/优先队列(第 9 章)。",
      "两端各司其职是队列的设计;栈的另一端是封死的,一切进出都发生在栈顶。",
    ],
    why: "栈只开一个口:push 和 pop 都在栈顶。最后压进去的盘子在最上面,自然最先被拿走 —— 这一条纪律换来了对嵌套、撤销类问题的完美贴合。",
  },
  {
    type: "choice",
    q: "用动态数组实现栈,栈顶应该放在数组的哪一端?为什么?",
    opts: [
      "尾部 —— 尾部追加/删除 O(1);放头部的话每次操作都要全体搬家 O(n)",
      "头部 —— 下标 0 访问起来更快",
      "哪端都一样,反正数组随机访问是 O(1)",
      "中间 —— 两边都留出余地更灵活",
    ],
    correct: 0,
    wrong: [
      undefined,
      "读 arr[0] 确实 O(1),但在头部「插入/删除」要把整个数组搬一格 —— 每次 push/pop 都变 O(n),完全没必要。",
      "随机访问快 ≠ 插删快:栈要的是频繁插删,而插删的成本取决于要不要搬动别的元素 —— 只有尾部是免搬家的。",
      "中间进出意味着两侧都可能要搬家,是最差的位置;栈只在一端操作,应选搬家成本为零的尾部。",
    ],
    why: "判断标准还是数组章那句话:要不要搬动别的元素?尾部操作谁也不惊动,所以数组栈的栈顶 = 尾部;链表栈则相反(栈顶 = 头节点),因为链表头插头删才是 O(1)。",
  },
  {
    type: "multi",
    q: "关于「空栈 pop」,下列说法正确的是?(多选)",
    opts: [
      "Java 的 ArrayDeque.pop() 会抛出 NoSuchElementException",
      "Python 的 list.pop() 会抛出 IndexError",
      "JavaScript 的 Array.pop() 不报错,静默返回 undefined",
      "三种语言都返回 null,所以不用判空",
    ],
    correct: [0, 1, 2],
    missHint:
      "三种语言的行为各不相同,但 A、B、C 说的都是事实 —— 对照 §05 的对照表再检查一遍你漏了哪个。",
    extraHint:
      "D 是错的:没有任何一种返回 null。尤其 JS 静默返回 undefined 最危险 —— bug 会潜伏到下游才爆炸。",
    why: "空栈 pop 是三语言行为差异最大的边界:Java/Python 当场报错,JS 沉默装死。所以任何栈代码动手前先想 isEmpty —— LC 20 里「栈空却遇到右括号要立即返回 false」就是这个边界。",
  },
  {
    type: "choice",
    q: "用单调栈解 LC 739(下一个更高温度),栈内自底向上维护的单调性是?",
    opts: [
      "温度递减 —— 更高温度一来,就把比它低的挨个弹出并当场结算答案",
      "温度递增 —— 保证栈顶永远是最高温度",
      "温度全部相等",
      "无所谓顺序,反正最后都会弹出来",
    ],
    correct: 0,
    wrong: [
      undefined,
      "递增栈找的是「下一个更小」(LC 84 柱状图用它);找「下一个更大」必须维护递减 —— 新来的比栈顶大,栈顶才算等到了答案。",
      "栈里存的是「还没等到更高温度」的候选,它们必须一个比一个矮,等待才有意义 —— 全相等没有信息量。",
      "顺序就是单调栈的全部:不维护单调性,「什么时候弹、弹出时结算什么」都无从谈起。",
    ],
    why: "口诀:找下一个更大 → 维护递减栈;找下一个更小 → 维护递增栈。答案在元素被弹出的那一瞬结算:「弹它的人」就是它等的答案。",
  },
  {
    type: "choice",
    q: "为什么 Java 刷题和工程里都不建议用 java.util.Stack?",
    opts: [
      "它继承 Vector:方法自带同步锁开销,还暴露按下标访问破坏 LIFO 纪律,是 JDK 1.0 的历史遗产",
      "它的 push/pop 是 O(n),太慢",
      "它在新版 JDK 里已被删除,用了编译不过",
      "它不能存对象,只能存 int",
    ],
    correct: 0,
    wrong: [
      undefined,
      "它的 push/pop 本身是均摊 O(1)(底层也是数组);慢在每个方法的 synchronized 锁,错在暴露 Vector 的全部接口,而不是复杂度。",
      "它还好好地待在 JDK 里,只是官方文档明确写着:栈操作应优先使用 ArrayDeque。",
      "它是泛型类 Stack<E>,什么对象都能装 —— 问题出在设计,不在类型。",
    ],
    why: "继承 Vector 意味着两件事:①每个方法都同步,单线程也要交「锁税」;②get(i)、insertElementAt 等数组式操作全部暴露,LIFO 纪律形同虚设。ArrayDeque 更快、接口更干净,是官方钦点的替代品。",
  },
  {
    type: "choice",
    q: "一个递归函数忘了写终止条件(或者递归 10 万层),最可能发生什么?",
    opts: [
      "栈溢出 StackOverflow —— 每层调用占一个栈帧,调用栈只有几 MB,塞满即崩",
      "CPU 过热,操作系统自动降频",
      "编译器会拒绝编译这段代码",
      "什么也不会发生,程序正常跑完",
    ],
    correct: 0,
    wrong: [
      undefined,
      "和温度无关 —— 崩溃是因为内存里划给调用栈的地盘(通常 1~8 MB)被栈帧塞满了。",
      "编译器只查语法,不追踪运行时会递归多少层 —— 死递归是完全合法的代码。",
      "每次调用都压入一个新栈帧,不 return 就永远不弹出,栈空间必然耗尽。",
    ],
    why: "函数每被调用一次就压一帧(局部变量 + 返回地址),递归 = 自己压自己。深度太大就是 StackOverflowError(Java)/ RecursionError(Python 默认限 1000 层)。解法:改迭代,或自己用显式栈模拟 —— 正是本章「用栈消除递归」的套路。",
  },
  {
    type: "fill",
    q: (
      <>
        单调栈扫描长度为 n 的数组时,每个元素最多入栈 ___ 次(填数字)——
        这正是它总复杂度是 O(n) 而不是 O(n²) 的原因。
      </>
    ),
    placeholder: "输入数字…",
    answers: ["1", "一", "一次", "1次"],
    hint: "想一想:一个元素被弹出之后,还有任何机会再回到栈里吗?",
    why: "每个元素恰好入栈 1 次、最多出栈 1 次,弹出即永别。n 个元素总操作 ≤ 2n —— 虽然代码里 while 套在 for 里,看「总账」仍是 O(n)。这跟数组扩容的均摊分析是同一招:别盯着最贵的一步,算全程平均。",
  },
];
