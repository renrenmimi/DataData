// 第 2 章 · 字符串 —— 题单与测验数据。
// 题单覆盖:对撞指针、计数数组、滑动窗口、模拟、KMP,从 Easy 铺到 Hard;
// hint 只给方向不剧透,key 用一段话把最优解讲透。

import type { Problem } from "@/lib/problems";
import type { QuizItem } from "@/lib/quiz";

export const PROBLEMS: Problem[] = [
  {
    lc: 344,
    title: "反转字符串",
    d: "easy",
    tags: ["对撞指针", "原地"],
    hint: "题目给的是字符数组(可变!)。两端各站一个指针,想想每一步该干什么。",
    key: (
      <>
        对撞指针:left 与 right 交换后同时向中间走,相遇即止。O(n) 时间、O(1)
        空间。注意题目特意给 char[]/list 而不是 str —— 因为字符串不可变,
        「原地反转字符串」这件事只能发生在它的孪生兄弟字符数组身上。
      </>
    ),
  },
  {
    lc: 242,
    title: "有效的字母异位词",
    d: "easy",
    tags: ["计数数组", "哈希思想"],
    hint: "异位词 = 每种字母出现次数完全相同。26 个小写字母,需要真的排序吗?",
    key: (
      <>
        开一个长度 26 的 int 数组当计数器:扫 s 时 count[c-&apos;a&apos;]++,扫 t 时
        count[c-&apos;a&apos;]--,最后全为 0 即异位词。O(n) 时间、O(26)=O(1) 空间,
        比排序法 O(n log n) 更快。「字符集有限 → 用数组代替哈希表」
        是字符串题的高频优化,LC 438、383 都靠它。
      </>
    ),
  },
  {
    lc: 205,
    title: "同构字符串",
    d: "easy",
    tags: ["双映射", "哈希思想"],
    hint: "「同构」要求映射是一一对应的:s→t 要唯一,t→s 也要唯一。只建一个方向的映射够吗?",
    key: (
      <>
        同时维护 s→t 和 t→s 两张映射表,逐位检查:若某位上任一方向与已有映射冲突,
        返回 false。只建单向映射会漏掉 &quot;badc&quot; vs &quot;baba&quot;
        这种「两个字符映射到同一个字符」的反例。O(n) 一次遍历。
      </>
    ),
  },
  {
    lc: 14,
    title: "最长公共前缀",
    d: "easy",
    tags: ["纵向扫描", "模拟"],
    hint: "别把字符串两两比较 —— 竖着看:先比所有串的第 0 位,再比第 1 位……",
    key: (
      <>
        纵向扫描:按列比较,第 i 列只要有一个串越界或字符不同,答案就是前 i
        个字符。最坏 O(S)(S 为所有字符总数),但通常在第一个分歧列就提前结束。
        也可以「以第一个串为标尺」逐个横向裁剪,复杂度相同。
      </>
    ),
  },
  {
    lc: 151,
    title: "反转字符串中的单词",
    d: "medium",
    tags: ["双指针", "两次翻转"],
    hint: "语言自带 split 能一行解决;进阶解法是「整体翻一次,每个单词再各自翻回来」。",
    key: (
      <>
        一行流:split 按空白切开 → 过滤空串 → 倒序 → join。追求 O(1) 额外空间
        (字符数组语言可行)则用三步翻转:先整体 reverse,再对每个单词局部
        reverse,最后原地压缩多余空格 —— 和 LC 189 轮转数组的翻转技巧一脉相承。
      </>
    ),
  },
  {
    lc: 438,
    title: "找到字符串中所有字母异位词",
    d: "medium",
    tags: ["定长滑窗", "计数数组"],
    hint: "异位词长度固定 = 窗口长度固定。窗口右移一格时,计数器只需要改动两个位置。",
    key: (
      <>
        定长滑动窗口 + 26 长度计数数组:窗口右移时「进一个字符 ++、出一个字符
        --」,再比较窗口计数与 p 的计数是否相等(或维护一个 diff/匹配数做到
        O(1) 判断)。O(n) 时间。「子串长度固定」是定长滑窗的强信号,
        比通用滑窗还少一个收缩循环。
      </>
    ),
  },
  {
    lc: 8,
    title: "字符串转换整数 (atoi)",
    d: "medium",
    tags: ["模拟", "边界处理"],
    hint: "难点不在算法在细节:空白、正负号、非法字符、溢出。按「状态」一步步来,别跳步。",
    key: (
      <>
        固定流程四步走:① 跳过前导空格;② 读一个可选的 +/- 号;③ 逐位读数字,
        遇非数字立刻停;④ 每次累加前先判溢出(ans &gt; (MAX-digit)/10 就该截断)。
        Python int 无限大可以最后再 clamp,Java/JS 必须边算边防。
        这题考的是把需求翻译成不重不漏的分支 —— 面试写崩率极高,值得亲手写三遍。
      </>
    ),
  },
  {
    lc: 28,
    title: "找出字符串中第一个匹配项的下标",
    d: "medium",
    tags: ["KMP", "子串匹配"],
    hint: "朴素 O(nm) 就能过,但这题存在的意义是让你写一遍 KMP:失配时,主串指针真的需要回退吗?",
    key: (
      <>
        KMP:先对 pattern 求 next 数组(每个前缀的最长「真前缀=真后缀」长度),
        匹配时主串指针 i 永不回退,失配只把 j 跳到 next[j-1] 继续比。
        预处理 O(m) + 匹配 O(n)。本章 §04 把 next 的直觉讲透了 ——
        背模板之前先能回答「为什么 j 跳到 next[j-1] 不会漏解」。
      </>
    ),
  },
  {
    lc: 6,
    title: "Z 字形变换",
    d: "medium",
    tags: ["模拟", "按行收集"],
    hint: "不要真的画出二维网格 —— 给每一行开一个收集器,一个「方向变量」上下反弹即可。",
    key: (
      <>
        开 numRows 个字符收集器(StringBuilder/list),用 row 指针和 dir=±1
        模拟 Z 字上下反弹:碰到第 0 行或最后一行就反向。每个字符恰好被收集一次,
        最后按行拼接,O(n)。数学解法(按周期 2·numRows-2 直接算下标)可作追问加分项。
      </>
    ),
  },
  {
    lc: 76,
    title: "最小覆盖子串",
    d: "hard",
    tags: ["滑动窗口", "计数数组", "need/have"],
    hint: "LC 3 的进阶版:窗口的合法条件从「无重复」换成「覆盖 t 的所有字符」。怎么 O(1) 判断覆盖?",
    key: (
      <>
        滑窗 + 两个计数器:need 记录 t 中每种字符还欠多少,have 记录已满足的字符
        种数。r 右扩补欠账,一旦 have == 种类总数(窗口合法),就收缩 l
        找更短答案,直到破坏合法性。每个字符最多进出窗口各一次,O(n)。
        滑窗三问(维护什么 / 何时扩 / 何时缩)在这题达到最完整的形态 ——
        掌握本题,滑动窗口的通用模板就完整了。
      </>
    ),
  },
];

export const QUIZ: QuizItem[] = [
  {
    type: "choice",
    q: "「字符串不可变(immutable)」的准确含义是?",
    opts: [
      "字符串对象一旦创建,内容永不改变;所有“修改”操作都返回一个新字符串",
      "字符串变量赋值之后就不能再指向别的字符串",
      "字符串不能参与拼接、替换等操作",
      "字符串只能存字母,不能存数字和符号",
    ],
    correct: 0,
    wrong: [
      undefined,
      "变量随时可以改指向(s = s + \"!\" 就让 s 指向了新串)—— 不可变说的是“对象内容”,不是“变量”。",
      "恰恰相反,拼接替换都可以做 —— 只是它们的结果是新造的字符串,原件分毫未动。",
      "字符串能装任何字符。不可变约束的是“能不能原地改”,和存什么内容无关。",
    ],
    why: "不可变 = 对象内容焊死。replace/toUpperCase/拼接看起来在“改”,其实都在重抄一份新的 —— 这一个事实推出了本章一半的复杂度结论(拼接 O(n+m)、循环 += O(n²))。",
  },
  {
    type: "choice",
    q: "在循环里执行 n 次 s += c(每次追加 1 个字符),总时间复杂度是?",
    opts: [
      "O(n²) —— 每次 += 都要重抄整个旧串,1+2+…+n 次拷贝",
      "O(n) —— 每次只加了 1 个字符",
      "O(n log n)",
      "O(1) —— 字符串操作都很快",
    ],
    correct: 0,
    wrong: [
      undefined,
      "「只加 1 个字符」是错觉:不可变意味着没法在旧串尾部续写,第 i 次 += 要新建长 i 的串并拷贝全部旧字符。",
      "log 来自“每步砍一半”的结构,这里是实打实的逐次全量重抄,是等差数列求和。",
      "单次看着快,累计成本是 1+2+…+n = n(n+1)/2 —— ConcatLab 里的计数器会告诉你差距有多夸张。",
    ],
    why: "第 i 次拼接拷贝约 i 个字符,总量 1+2+…+n ≈ n²/2。解法:Java 用 StringBuilder,Python/JS 用数组攒片段最后 join —— 都把总量压回 O(n)。",
  },
  {
    type: "choice",
    q: "Java 里比较两个字符串的内容,正确做法是?",
    opts: [
      "用 a.equals(b) —— == 比较的是引用(是不是同一个对象)",
      "用 a == b,简洁又正确",
      "先转成 char[] 再逐位比,别的都不可靠",
      "用 a.compareTo(b) > 0",
    ],
    correct: 0,
    wrong: [
      undefined,
      "== 只问“是不是同一个对象”。字面量因为常量池碰巧相等,new String() 立刻现原形 —— 测试全过、上线就炸的经典事故。",
      "equals 内部就是逐字符比较,已经替你写好了,不需要手工拆数组。",
      "compareTo 返回的是字典序差值(负/零/正),用 > 0 判断“相等”逻辑就错了;判相等用 equals。",
    ],
    why: "== 比引用,equals 比内容。字符串常量池会让 \"abc\" == \"abc\" 为 true,给人“== 能用”的错觉,一遇到 new String 或运行期拼接就会失效 —— 面试与线上事故双料高频。",
  },
  {
    type: "multi",
    q: "关于 UTF-8 编码,下列说法正确的有?(多选)",
    opts: [
      "ASCII 范围的字符(英文字母、数字)只占 1 个字节",
      "常用中文汉字通常占 3 个字节",
      "UTF-8 里每个字符固定占 2 个字节",
      "绝大多数 emoji 占 4 个字节",
    ],
    correct: [0, 1, 3],
    missHint: "回想 EncodeLab:输入 “A字🙂” 时,三个字符的字节数各是多少?1、3、4 —— 对应哪几个选项?",
    extraHint: "「固定 2 字节」描述的是 UCS-2/UTF-16 BMP 内的情况 —— UTF-8 的立身之本恰恰是“变长”:1 到 4 字节都有。",
    why: "UTF-8 按码点大小变长编码:≤U+007F 用 1 字节(完全兼容 ASCII),常用汉字落在 3 字节区,emoji 码点超过 U+FFFF 要 4 字节。“变长省空间 + 兼容 ASCII”正是它统治互联网的原因。",
  },
  {
    type: "choice",
    q: "为什么 Python 没有单独的 char(字符)类型?",
    opts: [
      "设计者认为长度为 1 的 str 足以胜任字符的所有职责,统一成一种类型更简单",
      "Python 性能太差,支持不了字符类型",
      "因为 Python 的字符串是可变的,不需要字符类型",
      "历史遗留 bug,官方一直没修",
    ],
    correct: 0,
    wrong: [
      undefined,
      "和性能无关 —— 这是刻意的语言设计:s[0] 返回一个长度为 1 的 str,所有字符串操作对它照常适用,不用学两套 API。",
      "Python 的 str 恰恰是不可变的;可变与否和有没有 char 类型是两回事。",
      "这是写进设计哲学的决定(“应该只有一种明显的做法”),不是 bug。",
    ],
    why: "Python 把“字符”统一为长度 1 的字符串:s[0]、切片、遍历返回的都是 str。代价是想拿“字符的数字身份”要显式调 ord()/chr(),而 Java/JS 里 char/charCodeAt 直接就是数字。",
  },
  {
    type: "choice",
    q: "看到什么信号,应该第一时间想到「滑动窗口」?",
    opts: [
      "求“连续子串/子数组”的最值,且窗口内维护的条件能随进出增量更新",
      "只要题目里出现“字符串”三个字",
      "需要枚举所有子序列(可以不连续)的时候",
      "数组必须先排好序的时候",
    ],
    correct: 0,
    wrong: [
      undefined,
      "字符串题还有对撞指针、计数、KMP、DP 等一大家子 —— 滑窗只认“连续 + 可增量维护”这两个特征。",
      "子序列可以跳着选,不是一段连续区间 —— 窗口滑不出“跳跃”,那是 DP/回溯的地盘。",
      "排序反而常常毁掉滑窗:窗口的意义就在于保持原序列的连续性和顺序。",
    ],
    why: "滑窗两大前提:① 答案是连续区间;② 窗口进/出一个元素时,维护的量(和、计数、去重集合)能 O(1) 更新。LC 3、209、438、76 全部命中这两条。",
  },
  {
    type: "fill",
    q: (
      <>
        JavaScript 里,<code>&quot;👍&quot;.length</code> 的值是多少?
      </>
    ),
    placeholder: "输入一个数字…",
    answers: ["2"],
    hint: "JS 的 length 数的是 UTF-16 编码单元(code unit),而 👍 的码点超出了 U+FFFF,需要一对“代理对”来表示。",
    why: "👍 的码点是 U+1F44D > U+FFFF,在 UTF-16 里要拆成两个编码单元(代理对),所以 length 是 2。想按“人眼字符”数,用 Array.from(s).length 或 for...of 按码点遍历。",
  },
];
