// 第 10 章 · 前缀树(Trie)—— 题单与测验数据。
// 题单聚焦「前缀匹配 / 字典树 / 0-1 Trie」三大套路,由易到难;
// hint 只给方向不剧透,key 用一段话把最优解讲透。

import type { Problem } from "@/lib/problems";
import type { QuizItem } from "@/lib/quiz";

export const PROBLEMS: Problem[] = [
  {
    lc: 208,
    title: "实现 Trie(前缀树)",
    d: "medium",
    tags: ["模板题", "字典树"],
    hint: "本章 §04 就是它的完整答案。节点 = children(Map/26 叉数组)+ isEnd 布尔位,三个方法都在「沿路径走」。",
    key: (
      <>
        insert:沿字符走,缺节点就新建,末节点 isEnd = true。search:走到底且
        isEnd 才算命中。startsWith:走得通即可,不看 isEnd。三个方法都是 O(L)(L
        = 词长),与词典里已有多少词无关 —— 这是 Trie 的招牌能力。所有后续 Trie
        题都在这三块骨架上加东西。
      </>
    ),
  },
  {
    lc: 211,
    title: "添加与搜索单词(数据结构设计)",
    d: "medium",
    tags: ["字典树", "DFS", "通配符"],
    hint: "普通字母照常走;遇到 '.' 时你不知道该走哪条边 —— 那就每条子边都试一遍。",
    key: (
      <>
        addWord 与 208 相同。search 改成递归 DFS(word, i, node):普通字符只往对应
        child 递归;遇到 '.' 就遍历<b>当前节点的所有 child</b> 分别递归,任意一条返回
        true 即命中。走到词尾看 isEnd。最坏(全是 '.')退化到 O(26^L),但真实数据里
        剪枝极快。这是「Trie + 回溯」的最小案例。
      </>
    ),
  },
  {
    lc: 648,
    title: "单词替换",
    d: "medium",
    tags: ["字典树", "前缀匹配"],
    hint: "把所有词根塞进 Trie。对句子里的每个单词,从头沿 Trie 走,第一次撞到 isEnd 就停 —— 那就是最短词根。",
    key: (
      <>
        用词根建 Trie。遍历句子每个单词,从 root 逐字符下沉:一旦当前节点 isEnd =
        true,说明找到一个词根前缀,用它替换并停止;若中途断路或走完仍没遇到词尾,保留原词。
        每个单词 O(其长度)。这题把「Trie 判前缀」用得最直白。
      </>
    ),
  },
  {
    lc: 677,
    title: "键值映射",
    d: "medium",
    tags: ["字典树", "前缀求和"],
    hint: "insert 时把 val 存在词尾;sum(prefix) = 先走到前缀节点,再把它子树里所有词的 val 加起来。",
    key: (
      <>
        节点上多存一个 val(词尾处)。sum(prefix):先 O(L) 走到前缀节点,再 DFS
        该节点整棵子树累加所有 val。进阶写法:insert 时若 key 已存在,沿路径给每个节点的
        「前缀累加值」加上 delta(新值−旧值),sum 就变成 O(L) 直接读。体会「在节点上挂聚合信息」的套路。
      </>
    ),
  },
  {
    lc: 720,
    title: "词典中最长的单词",
    d: "medium",
    tags: ["字典树", "DFS"],
    hint: "一个词能入选,要求它的每一个前缀也都是词典里的词 —— 也就是路径上每个节点都得 isEnd。",
    key: (
      <>
        建 Trie 后 DFS:只沿「isEnd = true 的子节点」往下走(保证每个前缀都是合法单词),
        记录能到达的最长单词;长度相同取字典序最小(按字符 a→z 顺序遍历子节点即可自然满足)。
        本质是在 Trie 上找「每一步都合法」的最长链。
      </>
    ),
  },
  {
    lc: 1268,
    title: "搜索推荐系统",
    d: "medium",
    tags: ["字典树", "自动补全"],
    hint: "这就是搜索框补全的原型:每输入一个字母,给出以当前已输入内容为前缀、字典序最小的 3 个商品。",
    key: (
      <>
        建 Trie,每个节点预存一个「该前缀下字典序最小的 ≤3 个词」列表(插入时维护,超过 3
        个就砍掉最大的)。用户逐字符输入时沿路径走,直接读节点上的列表;断路后续全为空。
        也可排序 + 二分,但 Trie 版把「前缀 → 候选」变成 O(1) 读取,正是工程里补全的做法。
      </>
    ),
  },
  {
    lc: 212,
    title: "单词搜索 II",
    d: "hard",
    tags: ["字典树", "网格回溯", "剪枝"],
    hint: "对每个词单独在网格里回溯会超时。反过来:把所有词建成一棵 Trie,让 DFS 沿着 Trie 走 —— 前缀不存在就立刻掉头。",
    key: (
      <>
        把 words 建成 Trie(词尾存整词)。从每个格子出发 DFS,<b>同步在 Trie 上下沉</b>:
        当前字母在 Trie 里没有对应 child 就<b>立刻剪枝回头</b>(这一步把指数级搜索压了下来);
        碰到词尾节点就收集该词并置空去重。Trie 让「多个词共享前缀路径」,一次网格遍历顺便查完所有词。
        Trie + 回溯 + 剪枝的集大成题。
      </>
    ),
  },
  {
    lc: 421,
    title: "数组中两个数的最大异或值",
    d: "hard",
    tags: ["0-1 Trie", "位运算", "贪心"],
    hint: "把每个数按二进制从高位到低位插进一棵「只有 0/1 两个分支」的 Trie。异或想变大,每一位都想找和自己相反的比特。",
    key: (
      <>
        建 0-1 Trie:每个数从最高位(如第 30 位)到最低位,依次按比特值走 0/1 分支插入。
        对每个数再走一遍,<b>贪心地每一位都优先拐向与自己相反的比特</b>(存在就能让该位异或得
        1),累积出它能凑到的最大异或值,取全局最大。O(32n) —— 把 O(n²) 的两两配对压成线性。
        这是 Trie 从「存字符串」扩展到「按位存整数」的经典跨界。
      </>
    ),
  },
];

export const QUIZ: QuizItem[] = [
  {
    type: "choice",
    q: (
      <>
        一棵 Trie 里存了 <b>50 万</b>个英文单词。查询单词 <code>apple</code> 是否存在,
        大致要比较多少次?
      </>
    ),
    opts: [
      "约 5 次(= 单词长度),和词典有多少词无关",
      "约 50 万次,要扫遍所有单词",
      "约 log₂(50万) ≈ 19 次",
      "约 26 × 5 次",
    ],
    correct: 0,
    wrong: [
      undefined,
      "Trie 不是线性扫描 —— 它顺着 a→p→p→l→e 这条路径逐字符下沉,只看 5 步就到底了。",
      "log n 是二分/平衡树的复杂度。Trie 的查询代价只由「词有多长」决定,和词的数量 n 无关。",
      "每一步是一次 child 查找(Map 命中 O(1) 或数组下标 O(1)),总步数就是词长 5,不乘 26。",
    ],
    why: "insert / search / startsWith 全是 O(L),L = 单词长度。词典从 1 个词涨到 50 万个词,查 apple 依旧是 5 步 —— 复杂度与词典大小无关,这是 Trie 最反直觉也最强的地方。",
  },
  {
    type: "choice",
    q: (
      <>
        Trie 里已插入 <code>cat</code> 和 <code>cattle</code>。如果节点<b>不设</b> isEnd
        标记,会发生什么?
      </>
    ),
    opts: [
      "无法区分「cat 是一个词」和「cat 只是 cattle 路上经过的前缀」,search(\"cat\") 会答错",
      "没影响,能走到底就说明是单词",
      "cattle 会存不进去",
      "会把 cat 和 cattle 当成同一个词",
    ],
    correct: 0,
    wrong: [
      undefined,
      "「能走到底」只能说明这是某个词的前缀。c-a-t 这三个节点同时也在 cattle 的路径上,没有 isEnd 就分不清 cat 到底是不是一个被插入过的完整单词。",
      "cattle 照样能插入 —— 它和 cat 共享 c-a-t 前缀,后面继续接 t-l-e 即可。问题只出在「查询」时分不清词与前缀。",
      "两者是不同长度的词,树里是一条延伸的路径,不会被当成同一个词;缺 isEnd 影响的是判定,不是存储。",
    ],
    why: "isEnd 标记回答的是「这个节点是不是某个单词的结尾」。cat 的 t 节点必须 isEnd = true,否则 search(\"cat\") 走到底看到 isEnd = false,会误判成「只是前缀」。cat / cattle 就是 isEnd 存在理由的教科书例子。",
  },
  {
    type: "choice",
    q: (
      <>
        下面哪种需求,是「该换 Trie、哈希表答不了」的最强信号?
      </>
    ),
    opts: [
      "「找出所有以 ca 开头的单词」这类前缀 / 补全查询",
      "判断某个单词在不在词典里",
      "统计每个单词出现了几次",
      "给单词随机取一个",
    ],
    correct: 0,
    wrong: [
      undefined,
      "「精确判断某个键在不在」正是哈希表的主场,O(1),不需要 Trie。",
      "计数用哈希表(词 → 次数)最直接,Trie 在这里没有优势。",
      "随机取值哈希表 / 数组都行,和前缀无关。",
    ],
    why: "哈希表把整个 key 打散成一个哈希值,天然丢掉了「开头」信息 —— 想找「以 ca 开头的词」只能遍历全表 O(N·L)。Trie 把相同前缀的词收拢在同一条路径上,走到 ca 节点,它整棵子树就是答案。看到「前缀 / 补全 / 共同开头」就想 Trie。",
  },
  {
    type: "multi",
    q: (
      <>
        关于 children 用「<b>26 叉数组 TrieNode[26]</b>」还是「<b>哈希表 Map</b>」,以下说法正确的有?(多选)
      </>
    ),
    opts: [
      "数组版:下标 c-'a' 直接定位,常数更小、访问最快",
      "数组版:即使某节点只有 1 个孩子,也固定占 26 个指针,稀疏时费内存",
      "Map 版:按需存储,省内存,还能天然支持大小写 / Unicode / 任意字符集",
      "Map 版的单次 child 查找是 O(词典大小)",
    ],
    correct: [0, 1, 2],
    missHint: "数组的「下标直达」和「固定 26 槽」这对优缺点,以及 Map 的「省内存 + 支持任意字符」,都是标准结论 —— 再看看漏了哪个。",
    extraHint: "最后一项错了:哈希表单次查 child 是均摊 O(1),和词典大小无关,别把它当成线性。",
    why: "26 叉数组:c-'a' 下标 O(1) 定位、常数极小,但每个节点无脑占 26 个指针,词稀疏时浪费严重;Map / dict:按实际孩子数存储、省内存、字符集不受限(中文、大小写混合都行),代价是哈希的常数略大。刷题小写字母用数组够快,工程 / 通用字符集用 Map 更稳。",
  },
  {
    type: "choice",
    q: (
      <>
        同一棵 Trie 上,<code>search(word)</code> 和 <code>startsWith(prefix)</code> 的唯一区别是?
      </>
    ),
    opts: [
      "两者都先沿路径走到底;search 还要求末节点 isEnd = true,startsWith 只要走得通就行",
      "search 从根走,startsWith 从叶子走",
      "startsWith 更慢,要遍历整棵子树",
      "search 是 O(L),startsWith 是 O(1)",
    ],
    correct: 0,
    wrong: [
      undefined,
      "两者都从 root 沿字符下沉,方向一样。区别只在到达终点后要不要检查 isEnd。",
      "startsWith 不需要遍历子树 —— 能从 root 走到 prefix 的末节点就返回 true,同样是 O(L)。",
      "两者都是 O(L)(L = 传入字符串长度),复杂度相同。",
    ],
    why: "它俩共用「从 root 逐字符走」的过程,复杂度都是 O(L)。search 到底后要额外确认末节点 isEnd(否则只是前缀不是词);startsWith 不看 isEnd,走得通即可。一行之差,却是「精确匹配」与「前缀匹配」的分界。",
  },
  {
    type: "fill",
    q: (
      <>
        为求「数组中两个数的最大异或值」(LC 421),我们把每个整数按二进制<b>逐位</b>插入一棵特殊的
        Trie。这棵每个节点只有 <code>0</code> 和 <code>1</code> 两个分支的树,通常被称为「____ Trie」。
        (填一个数字与斜杠组成的名字)
      </>
    ),
    placeholder: "例如 x/y 形式",
    answers: ["0/1", "01", "0-1", "0/1 trie", "01trie", "0-1trie", "二进制", "binary"],
    hint: "分支只有两种取值:比特 0 或比特 1。把这两个数字用斜杠连起来。",
    why: "0/1 Trie(也叫二进制 Trie / bitwise Trie):把整数当成固定长度(如 32 位)的比特串,从高位到低位插入,每层按该位是 0 还是 1 选分支。求最大异或时,每一位贪心地拐向「与自己相反的比特」,就能把 O(n²) 的两两配对压成 O(32n)。这是 Trie 从「存字符」推广到「存比特」的经典应用。",
  },
];
